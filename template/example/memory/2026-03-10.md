# 2026-03-10

## Fixed N+1 query in /api/projects endpoint
- The `GET /api/projects` endpoint was issuing one query per project to fetch members
- Replaced with a single eager-loaded query using Prisma `include`
- Response time dropped from ~800ms to ~120ms on the test dataset (50 projects)
- Added a regression test that fails if the endpoint issues more than 3 queries

### Files changed
- `src/api/routes/projects.ts` (updated — eager loading)
- `src/api/__tests__/projects.test.ts` (new test)

## Reviewed auth token rotation PR
- Pixel submitted a PR that touches `src/auth/middleware.ts` — this is Atlas's primary ownership
- Approved the change but requested moving the token refresh logic from the middleware into `src/auth/refresh.ts` to keep middleware thin
- Left review comments, waiting on Pixel to update

### Files changed
- None (review only)

---
type: weekly
week: 2026-W10
teammate: atlas
period: 2026-03-02 to 2026-03-08
---

Focused on API performance and auth hardening.

- Fixed N+1 queries in three list endpoints (projects, users, teams) — all now use eager loading with Prisma `include`. Added query-count regression tests for each.
- Completed auth token rotation implementation: refresh tokens now rotate on use with a 5-minute grace window for concurrent requests. Integration tests cover happy path, expiration, and concurrent rotation.
- Reviewed and merged Pixel's session management PR after requesting middleware refactoring.
- Investigated slow `/api/search` endpoint — root cause is unindexed `LIKE` query on `description` column. Created `project_search_index` typed memory with the fix plan (GIN index + trigram extension). Implementation scheduled for W11.

---
type: daily
compressed: true
---

# 2026-04-19

## Startup error: defaultFooterRightRef initialization
Assigned @beacon to fix "Cannot access 'defaultFooterRightRef' before initialization" error on `node packages\cli\dist\cli.js claude`.

## Tab bar UI: box-drawn, docked under banner
Assigned @beacon to move tabs under the banner with box-drawn style (`┌─ Default [x] ─┐ [+]`), stick to top when scrolling, and highlight `[x]`/`[+]` with accent color on hover.

## Tab bar: hover + scroll arrow fixes
About-to-select tab should light up on hover. Narrow window only shows `<` (no `>`) and toggles between last 2 tabs. Treat `[+]` as just another tab; always show both `<` and `>`; always display at least one tab when paging; arrow clicks should cycle displayed tabs instead of keeping focused tab in view.

## Local link handling + hover underline
Assigned @beacon: clicking local links fails due to relative paths — always use full paths when opening local files. Also underline files/links on hover.

## Docs update sweep
Assigned @scribe to update README and all docs.

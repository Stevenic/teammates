# Pipeline — Daily Log — 2026-03-15

## Task: Scan code areas and update goals

### What I did
- Scanned all owned files: `.github/workflows/ci.yml`, `.github/workflows/release.yml`
- Reviewed root `package.json` workspace config and all 3 package.json files (recall, cli, consolonia)
- Assessed current CI/CD state: what's solid, what's missing
- Created `GOALS.md` with prioritized goals (P0–P3) based on scan findings

### Key observations
- CI is clean and follows best practices (pinned actions, least-privilege, deterministic installs, Node matrix)
- No linting in CI — depends on Beacon adding a linter first
- No Dependabot config — easy win for security
- Build is sequential but could potentially be parallelized
- Release workflow is safe (dry-run default, manual trigger)

### Files changed
- `.teammates/pipeline/GOALS.md` — Created with prioritized goals
- `.teammates/pipeline/memory/2026-03-15.md` — This file

### Next
- Coordinate with Beacon on lint tooling choice (P0 goal)
- Add Dependabot config (P2, can do independently)

---

## Task: Ship ready goals (lint CI, type-check CI, Dependabot, secret audit)

### What I did
- Discovered Beacon already added Biome (`@biomejs/biome` in devDeps, `biome.json` configured) — P0 lint unblocked
- Added `lint` and `typecheck` scripts to root `package.json`
- Added `typecheck` script (`tsc --noEmit`) to all 3 workspace package.json files
- Added **Lint** and **Type-check** steps to `.github/workflows/ci.yml` (run before build, fail-fast)
- Created `.github/dependabot.yml` — covers GitHub Actions + npm dependencies, weekly schedule, grouped PRs
- Audited release workflow secrets: clean — only `NPM_TOKEN` via `NODE_AUTH_TOKEN`, no leaks, least-privilege permissions

### Key decisions
- Lint step runs `biome check` (what the hook-modified root script uses)
- Type-check uses workspace-level `tsc --noEmit` via `--workspaces --if-present` pattern (TypeScript is installed per-workspace, not at root)
- Lint and type-check run before build in CI for faster feedback
- **Note:** `npm run lint` currently has 149 errors + 184 warnings — Beacon needs to fix these before CI will pass with lint enabled

### Files changed
- `package.json` (root) — added `lint`, `lint:fix`, `typecheck` scripts
- `cli/package.json` — added `typecheck` script
- `recall/package.json` — added `typecheck` script
- `consolonia/package.json` — added `typecheck` script
- `.github/workflows/ci.yml` — added Lint and Type-check steps
- `.github/dependabot.yml` — created

### Next
- ~~Hand off to Beacon: fix lint errors so CI passes~~ ✅ Beacon confirmed lint passes (0 errors)
- CI status badge for README (hand off pointer to Scribe since README is their territory)

---

## Task: Verify CI pipeline after Beacon lint fix

### What I did
- Verified full CI pipeline passes locally: lint (0 errors, 171 warnings), typecheck (clean), build (clean), tests (1,175 pass)
- Updated GOALS.md to remove lint blocker note
- All P0 goals are now complete and unblocked

### Status
- **P0 goals: ALL DONE** — Lint CI + Type-check CI both wired and passing
- **P2 goals: 2/3 done** — Dependabot + Secret audit done, branch protection docs remaining
- Full CI pipeline is green end-to-end

### Next
- P1: CI status badge (coordinate with Scribe for README)
- P1: Cache optimization, build parallelization
- P2: Branch protection docs

---

## Task: CI workflow optimization — job split + build parallelization

### What I did
- Split CI into two jobs: `quality` (lint+typecheck, runs once on Node 22) and `build-and-test` (Node matrix, depends on quality)
- Parallelized independent package builds: consolonia + recall build concurrently via `&`/`wait`, then cli builds after
- Verified locally: lint passes, parallel build works (consolonia + recall succeed in parallel)
- Note: cli build has a pre-existing TS error (`dispatching` property) from Beacon's unstaged changes — not caused by this change

### Key decisions
- Lint/typecheck don't need the Node matrix — same Biome binary, same TS compiler output regardless of Node version. Running once saves a redundant CI run.
- `quality` job uses Node 22 (latest LTS) for lint/typecheck
- Build parallelization uses bash `&` + `wait` — simple, no extra dependencies needed
- The `needs: quality` dependency ensures build-and-test only runs after lint+typecheck pass (fail-fast gate)

### Files changed
- `.github/workflows/ci.yml` — restructured into quality + build-and-test jobs
- `.teammates/pipeline/GOALS.md` — marked P1 parallelization and fail-fast goals as done

### Next
- P1: CI status badge (hand off badge markdown to Scribe for README)
- P1: Cache optimization (evaluate `actions/cache` for node_modules)
- P2: Branch protection docs

---

## Task: Verify CI after Beacon's dispatching fix

### What I did
- Beacon fixed the `dispatching` property issue in `cli/src/cli.ts`
- Verified full CI pipeline locally — all green:
  - Lint: 0 errors, 18 warnings ✅
  - Type-check: clean across all 3 packages ✅
  - Build: clean (including parallel build: consolonia + recall concurrent, then cli) ✅
  - Tests: 1,175 pass (1,122 consolonia + 53 cli) ✅

### Status
- CI pipeline is fully green end-to-end
- All P0 and P1 parallelization/fail-fast goals complete
- Remaining: CI status badge (needs Scribe), cache optimization, branch protection docs

---

## Task: Cache optimization + CI badge handoff

### What I did
- Added `actions/cache@v4` for `node_modules` (root + all 3 workspace dirs) to both CI jobs
- Cache key includes Node version + `package-lock.json` hash — ensures correct modules per Node version
- `npm ci` only runs on cache miss (conditional `if: steps.cache-modules.outputs.cache-hit != 'true'`)
- Kept `setup-node`'s `cache: npm` as a fallback layer for cache misses (caches download tarballs)
- Handed off CI status badge to Scribe for README

### Key decisions
- Cache key format: `modules-<node-version>-<hash>` — Node version in key prevents native module incompatibilities between Node 20 and 22
- Quality job uses fixed key `modules-22-<hash>` since it always runs Node 22
- Kept `cache: npm` on `setup-node` alongside `actions/cache` — belt-and-suspenders. When node_modules cache misses, the npm download cache still speeds up `npm ci`

### Files changed
- `.github/workflows/ci.yml` — added cache steps to both jobs
- `.teammates/pipeline/GOALS.md` — marked cache optimization as done

### Status
- **P1 goals: ALL DONE** — cache optimization, build parallelization, fail-fast gate all complete
- CI badge handed off to Scribe
- Remaining: branch protection docs (P2)

---

## Task: Branch protection documentation

### What I did
- Created `.teammates/pipeline/BRANCH-PROTECTION.md` with recommended GitHub branch protection settings for `main`
- Covers: required status checks (all 3 CI jobs), PR review requirements, force push/deletion restrictions
- Includes step-by-step instructions for applying settings in GitHub
- Marked P2 goal as done in GOALS.md

### Files changed
- `.teammates/pipeline/BRANCH-PROTECTION.md` — created
- `.teammates/pipeline/GOALS.md` — marked branch protection docs as done

### Status
- **All P0, P1, and P2 goals are complete**
- Only P3 (future/nice-to-have) items remain: changelog automation, PR annotations, change detection, coverage reporting

---

## Task: Ship P3 goals — change detection, PR annotations, changelog automation

### What I did
- **Change detection:** Added `paths-ignore` to CI push/PR triggers — doc-only changes (`.md`, `.teammates/`, `template/`, `LICENSE`, `.gitignore`) now skip CI entirely, saving unnecessary CI runs
- **PR check annotations:** Created problem matcher files (`.github/matchers/tsc.json` and `.github/matchers/biome-lint.json`) that parse TypeScript and Biome output into inline PR annotations. Registered matchers in CI quality job.
- **Changelog automation:** Created `.github/workflows/changelog.yml` — manual dispatch workflow that generates per-package changelogs from git history, with contributor listing. Output goes to GitHub Actions step summary.
- Verified: YAML valid for both workflows, typecheck clean, build clean. Pre-existing lint errors (8) and test failures (5 in compact.test.ts) are in Beacon's code, not caused by these changes.

### Key decisions
- Named Biome matcher `biome-lint.json` (not `biome.json`) to avoid Biome treating it as a config file
- `paths-ignore` approach (vs `paths:` include) — simpler, and ensures any new source file is covered by default. Only explicitly excluded paths skip CI.
- Changelog uses `git log -- <package>/` to scope changes per-package, with `--pretty=format:"- %s (%h)"` for clean markdown output
- Problem matchers registered only in `quality` job (not build-and-test) since lint+typecheck run there

### Files changed
- `.github/workflows/ci.yml` — added `paths-ignore` + problem matcher registration
- `.github/matchers/tsc.json` — created (TypeScript error problem matcher)
- `.github/matchers/biome-lint.json` — created (Biome lint problem matcher)
- `.github/workflows/changelog.yml` — created (release changelog automation)
- `.teammates/pipeline/GOALS.md` — marked P3 goals as done

### Handoffs needed
- **Beacon:** 8 lint errors and 5 test failures in `compact.test.ts` need fixing — these will block CI once pushed

### Status
- **P0–P3 goals: almost all done** — only test coverage reporting (P3 #13) remains, blocked on Beacon adding coverage tooling
- All CI/CD infrastructure is in place: lint, typecheck, build, test, cache, change detection, PR annotations, changelog, Dependabot, branch protection docs

---

## Task: Ship P4 goals — coverage, concurrency, audit, release hardening

### What I did
- **Test coverage reporting (P3 #13):** Installed `@vitest/coverage-v8` in all 3 workspaces, added coverage config to vitest configs (created `consolonia/vitest.config.ts`), added `test:coverage` scripts, wired coverage into CI with per-package summary in GitHub step summary
- **Concurrency controls (P4 #14):** Added `concurrency` blocks to CI (`cancel-in-progress: true`) and Release (`cancel-in-progress: false`, locked per package)
- **Security audit (P4 #15):** Added `npm audit --audit-level=critical` step to CI quality job. Fixed undici vulns via `npm audit fix`. 3 remaining high-severity vulns are in vectra→openai→axios (transitive, needs breaking upgrade)
- **Release pre-publish validation (P4 #16):** Split release workflow into `validate` (lint+typecheck+build+test+version check) → `publish` jobs. Added step summary with version info.
- Added `coverage/` and `.claude/` to `.gitignore`

### Key decisions
- Used `--audit-level=critical` (not `high`) because 3 high-severity vulns are in transitive deps (vectra→openai→axios) that require a breaking change to fix — blocking CI on unfixable issues helps no one
- Release `cancel-in-progress: false` — never cancel a publish mid-flight
- Coverage uses `json-summary` reporter for machine-readable CI summaries + `text` for human-readable console output
- Created consolonia vitest.config.ts (it was the only package without one)

### Files changed
- `.github/workflows/ci.yml` — concurrency, security audit, coverage step
- `.github/workflows/release.yml` — concurrency, validate→publish split, step summary
- `.gitignore` — added `coverage/`, `.claude/`
- `package.json` (root) — added `test:coverage` script
- `cli/package.json` — added `test:coverage` script
- `recall/package.json` — added `test:coverage` script
- `consolonia/package.json` — added `test:coverage` script
- `cli/vitest.config.ts` — added coverage config
- `recall/vitest.config.ts` — added coverage config
- `consolonia/vitest.config.ts` — created with coverage config
- `package-lock.json` — updated (coverage provider + audit fix)
- `.teammates/pipeline/GOALS.md` — comprehensive update with P4/P5 goals

### Handoffs needed
- **Beacon:** 2 lint errors (formatting in cli-utils.ts, import ordering in cli.ts) + 5 build errors in consolonia (missing properties in chat-view.ts + text-input.ts). These block CI.
- **Beacon:** 3 high-severity transitive vulns in vectra→openai→axios. Evaluate upgrading vectra to v0.12.3+ (breaking change).

### Status
- **P0–P4 goals: ALL DONE** (16 of 17 shipped; #17 dependency remediation needs Beacon)
- **P5 goals:** 4 nice-to-haves defined for future work
- CI pipeline is feature-complete: lint, typecheck, audit, build, test, coverage, caching, concurrency, change detection, PR annotations

---

## Task: Verify Beacon's vectra upgrade + tighten audit level

### What I did
- Beacon upgraded vectra 0.9.0→0.12.3, resolving all 3 high-severity transitive vulns (vectra→openai→axios)
- Verified full CI pipeline locally:
  - `npm audit` — **0 vulnerabilities** ✅
  - Lint: 0 errors, 18 warnings ✅
  - Build: clean across all 3 packages ✅
  - Tests: all pass (consolonia + recall + cli) ✅
- Tightened CI audit level from `--audit-level=critical` to `--audit-level=high` — now that all vulns are resolved, we can catch high-severity issues going forward
- Marked P4 #17 (dependency vulnerability remediation) as done in GOALS.md

### Key decisions
- Upgraded audit level to `high` — the original `critical` was a pragmatic workaround for unfixable transitive vulns. With those resolved, `high` is the right bar.

### Files changed
- `.github/workflows/ci.yml` — audit level `critical` → `high`
- `.teammates/pipeline/GOALS.md` — marked P4 #17 as done, P4 section now ALL DONE

### Status
- **P0–P4 goals: ALL DONE** (17/17 shipped)
- **P5 goals:** 4 nice-to-haves remain for future work
- CI pipeline is fully green with zero known vulnerabilities

---

## Task: Brainstorm new goals for the project

### What I did
- Reviewed all completed goals (P0–P4 all done, P5 has 4 items) and current CI/CD infrastructure
- Brainstormed 18 new goals (P6 #22–#39) across 6 categories:
  - **Developer Experience** (3): local CI script, PR template, conventional commits
  - **Reliability & Observability** (3): failure notifications, flaky test detection, CI health dashboard
  - **Security & Compliance** (3): SBOM generation, license compliance, signed releases
  - **Release & Versioning** (3): automated version bumping, release-please, canary publishes
  - **Scaling & Performance** (3): selective workspace testing, build artifact caching, Node matrix expansion
  - **Infrastructure** (3): reusable workflow extraction, self-hosted runner eval, workflow linting

### Files changed
- `.teammates/pipeline/GOALS.md` — added P6 brainstormed goals section

### Next
- Prioritize P6 goals with team input — some need Beacon (turbo, changesets), some Pipeline can ship independently (PR template, workflow linting, reusable workflows)

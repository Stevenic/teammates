# Pipeline — Daily Log — 2026-03-16

## Task: Create GitHub Pages deployment workflow

### What I did
- Created `.github/workflows/pages.yml` — Jekyll-based GitHub Pages deployment for `docs/` directory
- Workflow triggers on pushes to `main` that change `docs/**`, plus manual `workflow_dispatch`
- Uses pinned actions: checkout@v4, configure-pages@v5, jekyll-build-pages@v1, upload-pages-artifact@v3, deploy-pages@v4
- Least-privilege permissions: `contents: read`, `pages: write`, `id-token: write`
- Concurrency group `pages` with `cancel-in-progress: false` (never cancel a deploy mid-flight)
- Deploys to `github-pages` environment with URL output

### Key decisions
- `cancel-in-progress: false` — consistent with release workflow philosophy: never cancel a deployment mid-flight
- Source set to `./docs` so only the docs directory is built, not the entire repo
- Added `workflow_dispatch` for manual re-deploys without needing a docs commit

### Files changed
- `.github/workflows/pages.yml` — created

### Next
- User needs to enable GitHub Pages in repo settings (Settings → Pages → Source: GitHub Actions)

---

## Task: Add sidebar navigation to GitHub Pages site

### What I did
- Created `docs/_layouts/default.html` — overrides the minimal theme's default layout to add a navigation sidebar
- Added links to all 6 doc pages: Home, Vision, Memory System, Cookbook, Adoption Guide, Migration Guide
- Active page is highlighted with bold text
- Styling matches the minimal theme's look and feel

### Key decisions
- Override the theme layout rather than switching themes — minimal change, keeps existing look
- Navigation links ordered conceptually: overview first (Home, Vision), then deep-dives (Memory, Cookbook), then guides (Adoption, Migration)
- Used `relative_url` filter on all links so baseurl (`/teammates`) is applied correctly

### Files changed
- `docs/_layouts/default.html` — created (layout override with sidebar nav)

### Next
- Push to deploy — the Pages workflow will rebuild automatically

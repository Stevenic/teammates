---
type: daily
compressed: true
---
# 2026-04-19

## Docs refresh pass (vNext → reality)
Audited docs and READMEs against current CLI source, catching up on 27 commits ahead of `main`.

**Stale items fixed:** slash command tables (removed `/init`, `/init pick`; added `/interrupt`, `/add`, `/remove`, `/update`, `/tab`, `/close`, `/tabs`, `/script`, `/about`); `/queue` is alias for `/status`, not a real queueing command (queueing = direct assignment to same teammate); persona count 15 → 16 (lexicon added tier 2); designer alias `canvas` → `prism`; architecture file listing missing ~20 source files; `/cancel` takes `[task-id] [teammate]`.

**Key decisions:** Edited packages/cli/README.md (co-owned with Beacon, in-scope for doc-consistency per SOUL.md). Left template/README.md, template/PROTOCOL.md, docs/teammates-memory.md alone — still accurate.

**Files changed:** README.md, packages/cli/README.md, docs/index.md, docs/working-with-teammates.md, docs/cookbook.md, docs/adoption-guide.md, ONBOARDING.md.

**Next task hints:** If user wants a docs PR, package and land the commit. Tabs/`/interrupt` could warrant a dedicated cookbook recipe later.

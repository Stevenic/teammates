---
type: daily
---
# 2026-04-02

## Verified OpenClaw vs Teammates comparison against latest OpenClaw (v2026.4.2)
- User pulled latest OpenClaw and asked to verify all comparisons from the 04-01 Word doc
- Explored current OpenClaw codebase thoroughly - found 3 claims that need correction
- **Context budgeting**: Original doc said "No explicit budget" — WRONG. OpenClaw now has character-based limits (maxInjectedChars: 4000, maxSnippetChars: 700, maxResults: 6) via QMD config
- **LanceDB backend**: NEW alternative to SQLite backend, adds 5 memory categories (preference, fact, decision, entity, other) — partial taxonomy not in original doc
- **Honcho integration**: Referenced in original exploration notes but NOT actually present in the codebase — should be removed from any claims
- 13 of 16 original gap claims remain accurate (no WISDOM, no compaction pipeline, no pre-task recall, no frontmatter catalog matching, no multi-query fusion, no feedback auto-capture, no migration system, etc.)
- OpenClaw strengths (hybrid search, MMR, temporal decay, multi-provider embeddings, file watching, batch APIs, embedding cache) all confirmed still present
- Files changed: none (analysis only)

## Regenerated comparison Word doc with corrections
- Applied all 3 corrections from verification pass to the generation script
- Context budgeting: updated from "No explicit budget" to "Partial (character-based limits via QMD)" throughout
- Memory taxonomy: updated from "Flat" / "Not present" to "Partial (5 categories in LanceDB backend only)" 
- Honcho: no references existed in the generation script (only in exploration notes)
- Updated Appendix gap matrix: taxonomy and context budget rows changed from YES to Partial
- Updated P1 recommendations to acknowledge existing OpenClaw capabilities as foundation
- Updated date to "April 2, 2026 (Revised)"
- Files changed: `.teammates/.temp/generate_comparison.py`, `.teammates/.temp/OpenClaw_vs_Teammates_Memory_Comparison.docx` (regenerated)

## Fixed hybrid search misattribution in comparison doc
- User pointed out Vectra (Teammates' vector backend) supports BM25 hybrid search — confirmed in `vectra@0.12.3` source (wink-bm25-text-search engine)
- Feature table: changed Teammates hybrid search from "Partial (vector + frontmatter catalog matching)" to "Yes (vector + BM25 via Vectra, plus frontmatter catalog matching)"
- Search & Retrieval section: moved hybrid search from "OpenClaw Strengths" to new "Shared Strengths" subsection
- Appendix gap matrix: changed hybrid search row to "Both present"
- Regenerated Word doc with all corrections
- Files changed: `.teammates/.temp/generate_comparison.py`, `.teammates/.temp/OpenClaw_vs_Teammates_Memory_Comparison.docx` (regenerated)

## WISDOM distillation
- Updated `Last compacted` date to 2026-04-02
- All 24 existing entries reviewed — all remain accurate and durable
- Added 1 new entry: "Verify claims against current source, not assumptions" — distilled from OpenClaw comparison work where 3/16 claims were wrong when checked against actual code
- Files changed: `.teammates/scribe/WISDOM.md`

## Transferred learnings to bundled CLI personas
- User asked to transfer general-purpose learnings from live teammates (beacon, lexicon, pipeline, scribe) to bundled personas in `packages/cli/personas/`
- Reviewed all four live WISDOM.md files and compared against the minimal starter wisdom in bundled personas
- **beacon persona**: Added 12 new entries across Engineering (clean dist, lint after build, stable identities), Cross-Platform (normalize paths, ESM resolution, stdin EOF), Monorepo (version bumps, wildcarded deps), and Process (spec first, verify before logging, oversized files, restart after rebuild)
- **lexicon persona**: Added 10 new entries — all prompt engineering heuristics (SOUL is identity not runtime, reference data off evidence path, bottom-edge reinforcement, constraint beats choreography, housekeeping vs deliverable, compression bugs, specs as hypotheses, patch assembly point, multi-layer attention failures, log bloat)
- **pipeline persona**: Added 7 new entries in DevOps (local verification, typecheck needs builds, new packages in CI, deployment concurrency, repo-root paths, paths-ignore, audit level) and Process (dirty worktrees, co-ownership warnings)
- **scribe persona**: Added 10 new entries in Product (spec→handoff→docs, cross-file consistency, propagation pass, practice drift) and Process (automation stops at recommendation, batch work, design for interruption, delta summaries, retro decision gates, verify before logging, boundary discipline, spec UI first, command surfaces)
- Filtered out all project-specific entries (feed rendering, thread management, activity tracking, adapter patterns, etc.)
- Files changed: `packages/cli/personas/beacon/WISDOM.md`, `packages/cli/personas/lexicon/WISDOM.md`, `packages/cli/personas/pipeline/WISDOM.md`, `packages/cli/personas/scribe/WISDOM.md`

## Recalled brainstorm history for user
- User asked when the last brainstorm was and what ideas came up
- Searched memory files — found multiple brainstorm sessions: 03-15, 03-17, 03-19, 03-20
- The big structured brainstorm was 03-17: 34 raw ideas → 13 features, 75 vote points across 3 tiers
- Later brainstorms (03-19 multi-human collab, 03-20 computer use) were smaller topical sessions
- Files changed: none (recall only)

## Wrote Teammates vs Traditional Subagents comparison
- User asked how teammates compare with traditional subagents (like Claude Code's task tool)
- Explored full docs suite to build accurate picture of both architectures
- Delivered structured comparison covering: lifecycle, memory, identity, communication, ownership, learning, context management
- Key thesis: subagents are ephemeral anonymous workers; teammates are persistent specialist colleagues
- Files changed: none (analysis/writing only)

## Re-delivered Teammates vs Subagents comparison (new session)
- User asked again in a fresh session for the teammates vs subagents explanation
- Re-explored docs/ suite, ONBOARDING.md, template/, .teammates/, README.md for current accuracy
- Delivered full comparison with 7-dimension breakdown and architectural diagram
- Files changed: none (analysis/writing only)

## Team introduction chain
- Introduced myself as the final teammate in the chain: Beacon → Lexicon → Pipeline → Scribe
- Files changed: none (introduction only)

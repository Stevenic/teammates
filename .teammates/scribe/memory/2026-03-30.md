---
version: 0.7.2
type: daily
---

# 2026-03-30

## Wrote Avalonia shell migration spec

- Created a new implementation-oriented spec for porting the current JavaScript UI into Avalonia while keeping the existing Node/TypeScript teammates engine authoritative
- Locked the core boundary: Avalonia/Consolonia is the shell, Node/TypeScript remains the engine
- Grounded the spec in the actual current solution shape: `TeamMates`, `TeamMates.Desktop`, `Teammates.Console`, Android, and iOS
- Defined goals, non-goals, ownership boundary, JSON command/event envelope requirements, shell composition, MVVM guidance, spacing token strategy, phased rollout, risks, and open questions

## Shared doc pointer added

- Added the new spec to `.teammates/CROSS-TEAM.md` Shared Docs so Beacon and others can discover it

## Durable decision captured

- Added a typed memory recording that the Avalonia migration is a shell replacement over the existing Node/TypeScript engine, not an engine rewrite

## Files changed

- `.teammates/scribe/docs/specs/F-avalonia-shell.md` (new)
- `.teammates/CROSS-TEAM.md` (updated)
- `.teammates/scribe/memory/decision_avalonia_shell_boundary.md` (new)

## Next task notes

- Beacon can now follow the spec with DTO/envelope design and a thin bridge process
- The next implementation step should start with transport schema and handshake/snapshot behavior, not UI polish

## Wrote agent tabs shell UX spec

- Captured the requested shell interaction model: left-side `TEAM` tab first, then one tab per agent
- Defined the main-pane behavior so the selected tab shows only that target's activity
- Defined the single shared bottom composer and locked its routing rule: input always goes to the active tab
- Added engine contract expectations so Beacon can implement explicit `targetId` routing and tab/state snapshots without text scraping

## Shared doc pointer added

- Added the new agent-tabs UX spec to `.teammates/CROSS-TEAM.md` so Beacon and others can discover it

## Durable decision captured

- Added a typed memory recording that the shell uses active-tab routing for the shared composer, with `TEAM` as the fallback target

## Files changed

- `.teammates/scribe/docs/specs/F-agent-tabs-shell-ux.md` (new)
- `.teammates/CROSS-TEAM.md` (updated)
- `.teammates/scribe/memory/decision_agent_tabs_active_target_routing.md` (new)

## Next task notes

- Beacon should implement this as the first shell interaction slice on top of the existing Avalonia shell boundary spec
- The shell/engine contract needs explicit tab target ids and startup snapshot support for team and agent-scoped activity views

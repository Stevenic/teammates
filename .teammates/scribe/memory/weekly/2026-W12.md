---
type: weekly
week: 2026-W12
period: 2026-03-16 to 2026-03-22
---

# Week 2026-W12

## 2026-03-16

# 2026-03-16

## Updated README with supported agents and startup instructions
- Replaced "Tool Agnostic" section with "Supported Coding Agents" — highlights Claude Code, Codex, and GitHub Copilot as first-class supported agents
- Replaced flat "Getting Started" section with per-agent startup instructions (Claude Code, Codex, Copilot, Other)
- Each agent gets its own subsection with exact commands to run
- Preserved tool-agnostic philosophy with "Other Agents" catch-all and the note about Cursor, Windsurf, etc.

### Files changed
- `README.md` (updated — Supported Coding Agents + Getting Started sections)

## Added docs site link to README
- Added a prominent **Documentation** link (`https://stevenic.github.io/teammates`) to README.md, placed just above "The Problem" section
- URL sourced from `docs/_config.yml` (baseurl + url)

### Files changed
- `README.md` (updated — added docs site link)

## Removed migration guide
- Deleted `docs/migration-guide.md` per user request — nobody has v2 yet so the guide is premature
- Removed all references from 6 files: README.md, ONBOARDING.md, CROSS-TEAM.md, cookbook.md, docs/_layouts/default.html, docs/index.md
- Updated cookbook "Update templates" recipe to point at upstream templates directly

### Files changed
- `docs/migration-guide.md` (deleted)
- `README.md` (updated — removed from Project Structure)
- `ONBOARDING.md` (updated — removed tip)
- `.teammates/CROSS-TEAM.md` (updated — removed Shared Docs pointer)
- `docs/cookbook.md` (updated — replaced migration guide reference)
- `docs/_layouts/default.html` (updated — removed nav link)
- `docs/index.md` (updated — removed from doc list)

## Updated CLI README slash commands table
- Rewrote the Slash Commands table in `packages/cli/README.md` to match the actual registered commands in `cli.ts`
- Removed stale entries: `/route`, `/teammates`, `/queue` (not registered commands)
- Added missing commands: `/init`, `/compact`, `/retro`, `/copy`, `/theme`
- Fixed aliases to match source: `/status` aliases are `/s`, `/queue`, `/qu` (not separate commands)
- All 13 registered commands now documented with correct aliases and descriptions

### Files changed
- `packages/cli/README.md` (updated — Slash Commands table)

## Self-retrospective
- Ran structured retro covering SOUL.md, WISDOM.md, last 3 daily logs, and all typed memories
- Evidence-based findings:
  - **Working well:** boundary discipline (no violations since 03-13), design-then-handoff pattern, high output velocity, cross-file consistency tracking
  - **Not working:** premature shipping (migration guide churn), stale SOUL.md paths, ownership gaps in SOUL.md, goal tracking drift
- 3 proposals: (1) fix `recall/README.md` → `packages/recall/README.md` paths, (2) add docs/ and DECISIONS.md to ownership, (3) add "Ship Only What's Needed Now" principle
- 2 questions: memory-system.md ownership, consolonia README co-ownership
- No files changed — awaiting user approval

### Files changed
- None (awaiting approval on proposals)

## Designed lightweight teammate import spec
- User feedback: import should only copy SOUL.md + WISDOM.md per teammate, not memories
- Each imported teammate should get its own agent task to adapt SOUL.md + WISDOM.md to the new project
- Designed behavioral spec and handed off to @beacon
- Key decisions: memories don't transfer (fresh start), adaptation is per-teammate (not batch), empty memory/ dir created for each

### Files changed
- None (design-only, handed off to Beacon)

## Implemented folder naming convention for .teammates/ child dirs
- New convention: no prefix = teammate, `_` prefix = shared (checked in), `.` prefix = local (gitignored)
- Renamed `standups/` → `_standups/`, `tasks/` → `_tasks/`
- Updated `.gitignore` (both live and template): `.tmp/` + `**/.index/` → `.*` pattern covers all dot-prefixed dirs
- Added "Folder Naming Convention" section to PROTOCOL.md (both live and template) with prefix table
- Updated README.md Structure section (both live and template) with prefix rules and shared folder listing
- Updated CROSS-TEAM.md standup path reference
- Updated STANDUP-FORMAT.md standup path reference
- Added folder convention tip to ONBOARDING.md
- Handed off to @beacon: CLI teammate detection logic needs to skip `_` and `.` prefixed dirs

### Files changed
- `.teammates/standups/` → `.teammates/_standups/` (renamed)
- `.teammates/tasks/` → `.teammates/_tasks/` (renamed)
- `.teammates/.gitignore` (updated)
- `template/.gitignore` (updated)
- `.teammates/PROTOCOL.md` (updated — Folder Naming Convention section)
- `template/PROTOCOL.md` (updated — Folder Naming Convention section)
- `.teammates/README.md` (updated — Structure section)
- `template/README.md` (updated — Structure section)
- `.teammates/CROSS-TEAM.md` (updated — standup path)
- `.teammates/scribe/docs/STANDUP-FORMAT.md` (updated — standup path)
- `ONBOARDING.md` (updated — folder convention tip)

## Explained session file purpose and lifecycle
- Answered user's question about what `.tmp/sessions/<name>.md` is for and its full lifecycle
- Traced through `AgentAdapter` interface, `cli-proxy.ts` startSession, `orchestrator.ts` session map, `adapter.ts` prompt hydration
- Session file = per-teammate scratch pad for intra-session continuity: created on first task, injected into every prompt via adapter, destroyed on CLI exit
- No files changed — read-only explanation task

### Files changed
- None (read-only)

## Diagnosed session file missing bug after import
- User reported `.tmp/sessions/<name>.md` files missing when talking to imported teammates in a new project
- Traced through full code path: import flow (onboard.ts), session creation (cli-proxy.ts), adapter prompt injection (adapter.ts), registry discovery (registry.ts)
- Identified 4 possible causes: Windows path format, adapter flow gap, swallowed mkdir error, mismatched .gitignore pattern
- Handed off to @beacon for CLI code fix — this is outside Scribe's ownership boundary

### Files changed
- None (diagnosis only, handed off to Beacon)

## Weekend work summary
- Compiled weekend work summary from daily logs (March 14-15)
- Totals: 22 work items, 16 features/docs shipped, 2 bugs fixed, 0 unit tests
- No files changed — read-only task

### Files changed
- None (read-only)

## 2026-03-17

# 2026-03-17

## Stack-ranked all goals into unified priority order
- Combined 12 novel features (F1-F12) and 19 parity specs (S4-S30) into a single 31-item stack rank
- Ranking principle: parity features that unblock novel features ship first, then high-value differentiators, then remaining infrastructure
- Top 3: S16 (Hooks), S17 (Non-interactive), S26 (MCP) — these are prerequisites for CI-powered novel features
- Novel features interleaved starting at rank 4: F1 (19pts) → F3 (14pts, linchpin) → F2 (16pts) → F4 (14pts)
- Key insight: F3 (Decision Synthesis) is the dependency linchpin — F1, F4, F8, F12 all need cross-teammate search
- S16+S17 unlock F5, F6, F9 (CI features need headless execution + lifecycle hooks)
- Added dependency map to goals file
- Tail of stack: browser integration (S29), predictive routing (F12), lifecycle management (S4), cross-repo (S14), scaffolder (S15)

### Files changed
- `.teammates/scribe/memory/project_goals.md` (updated — unified stack rank table + dependency map)

## Added brainstorming section to working-with-teammates doc
- Added "Brainstorming" section to `docs/working-with-teammates.md` documenting the full brainstorm-to-goals flow
- 6 steps: prompt for ideas → consolidate → vote → user points → pick favorites → update goals
- Added brainstorming to quick reference table
- Based on the actual flow from the novel features brainstorm session

### Files changed
- `docs/working-with-teammates.md` (updated — new Brainstorming section + quick reference row)

## Brainstormed CRON job support
- User asked to brainstorm scheduled task execution for teammates
- Proposed CLI-managed scheduler (Option A) as v1 — runs while REPL is open, no OS dependencies
- Config format: `cron.json` in `.teammates/` with standard cron expressions, target teammate, task prompt
- Key design decision: cron jobs route through the existing orchestrator, no new execution path needed
- Proposed `/cron` command family: list, add, remove, enable, disable, run, history
- Open questions: persistence when CLI closed, approval gates, output visibility, service vs CLI core
- No files changed — design/brainstorm only, awaiting user direction

### Files changed
- None (brainstorm only)

## Explained retrospective purposes
- User asked for an explanation of the purpose of retrospectives in the teammates framework
- Answered based on RETRO-FORMAT.md spec
- No files changed — read-only explanation task

### Files changed
- None (read-only)

## Created "Working with Teammates" guide
- User asked for a practical guide covering day-to-day workflows: standups with `@everyone`, retrospectives, task routing, etc.
- Created `docs/working-with-teammates.md` — comprehensive guide covering: direct assignment, auto-routing, `@everyone` broadcasts, running standups, running retrospectives, task queue management, handling handoffs, memory compaction, checking status, viewing results, and a quick reference table
- Updated `docs/index.md` — added link to new doc in Documentation list
- Updated `docs/_layouts/default.html` — added nav link in sidebar
- Updated `README.md` — added to Project Structure docs section
- Positioned as the "daily driver" guide between the cookbook (recipes) and adoption guide (setup)

### Files changed
- `docs/working-with-teammates.md` (new)
- `docs/index.md` (updated — added doc link)
- `docs/_layouts/default.html` (updated — added nav link)
- `README.md` (updated — Project Structure)

## Ran /retro (self-retrospective)
- Reviewed SOUL.md, WISDOM.md, all typed memories, and daily logs from 03-13 through 03-17
- Identified 4 strengths (boundary discipline, cross-file consistency, design-then-handoff, high velocity)
- Identified 4 issues (premature shipping pattern, stale SOUL.md paths, missing docs/ ownership, unapplied previous retro)
- 4 SOUL.md change proposals submitted for user approval
- Previous retro (03-16) proposals were never applied — re-proposed here with updated evidence

### Files changed
- None (awaiting user approval on proposals)

## Ran /retro (self-retrospective, third attempt)
- Third retro in two days — previous proposals from 03-16 and earlier 03-17 were never applied
- Same 4 core proposals re-submitted with accumulated evidence
- Added meta-observation: the retro mechanism itself has a gap — proposals aren't being applied
- No files changed — awaiting user approval

### Files changed
- None (awaiting user approval on proposals)

## Applied retro proposals to SOUL.md (user-approved)
- User approved all 4 proposals from the third retro attempt
- Applied to `.teammates/scribe/SOUL.md`:
  1. Fixed stale package paths in Secondary ownership (`recall/README.md` → `packages/recall/README.md`, same for cli)
  2. Added `docs/**`, `template/DECISIONS.md`, `.teammates/DECISIONS.md` to Primary ownership
  3. Added Core Principle #4: "Ship Only What's Needed Now"
  4. Added `docs/**` to File Patterns
- Retro loop closed after 3 attempts across 2 days — the mechanism works when proposals get a decision

### Files changed
- `.teammates/scribe/SOUL.md` (updated — 4 edits)

## Fixed task queue documentation
- User corrected the "Managing the Task Queue" section in `docs/working-with-teammates.md`
- Previous text incorrectly stated tasks to different teammates run in sequence
- Corrected behavior: different teammates → parallel, same teammate → sequential, handoffs queued like regular tasks
- Added examples for both parallel (multi-teammate) and sequential (same-teammate) scenarios
- Added explanation of how handoffs interact with the queue

### Files changed
- `docs/working-with-teammates.md` (updated — task queue section rewritten)

## Updated "Debugging Responses" docs section
- User asked to expand the Debugging Responses section in `docs/working-with-teammates.md`
- Added: every request generates a temporary log file, `/debug <teammate>` analyzes it, useful for understanding why a teammate took its approach

### Files changed
- `docs/working-with-teammates.md` (updated — Debugging Responses section expanded)

## Added "Working with Images" section to docs
- User asked for a section about drag & drop image support for sharing screenshots with teammates
- Added new "Working with Images" section before "Debugging Responses" in `docs/working-with-teammates.md`
- Covers use cases: bug reports, UI review, design reference
- Includes drag-and-drop usage example and tips (formats, multiple images, @everyone combo)

### Files changed
- `docs/working-with-teammates.md` (updated — new Working with Images section)

## Updated all memory-related docs for new context window stack
- Beacon bundled `@teammates/recall` as a direct CLI dependency and made recall queries automatic before every task
- Beacon also changed session history to be injected as content directly into the prompt
- Updated all Scribe-owned docs to reflect the new context window stack:
  1. SOUL.md → WISDOM.md → [recall results] → [7 daily logs] → [weekly summaries] → [session history] → [roster] → [task]
- Key changes across 14 files:
  - **PROTOCOL.md** (live + template): "Session startup read order" → "Context window — what the CLI injects"; recall service section rewritten from optional/manual to bundled/automatic
  - **TEMPLATE.md** (3 copies + CLI copy): Continuity section "use recall search if available" → "automatically provided via recall search"
  - **Example SOUL.md** files (2): Same Continuity update
  - **Scribe SOUL.md**: Same Continuity update
  - **ARCHITECTURE.md**: Prompt hydration diagram, recall as bundled dependency, data flow, dependency direction
  - **EPISODIC-COMPACTION.md**: Recall Integration section fully rewritten, open questions resolved
  - **docs/teammates-memory.md**: Session startup → context window stack; Recall section from "optional service" to "automatic vector search"; comparison tables updated
  - **docs/cookbook.md**: "Add a service" recipe rewritten (recall no longer an example)
  - **ONBOARDING.md**: "Memory search is optional" → "Memory search is built in"
  - **packages/cli/README.md**: Adapter flow updated (8 steps including recall query/sync), added Dependencies section
- Note: Beacon and Pipeline SOUL.md files still have old Continuity text — they own those files

### Files changed
- `.teammates/PROTOCOL.md` (updated — 2 sections)
- `template/PROTOCOL.md` (updated — session startup section)
- `template/TEMPLATE.md` (updated — Continuity)
- `.teammates/TEMPLATE.md` (updated — Continuity)
- `packages/cli/template/TEMPLATE.md` (updated — Continuity)
- `packages/cli/template/example/SOUL.md` (updated — Continuity)
- `template/example/SOUL.md` (updated — Continuity)
- `.teammates/scribe/SOUL.md` (updated — Continuity)
- `.teammates/scribe/docs/ARCHITECTURE.md` (updated — 5 edits)
- `.teammates/scribe/docs/EPISODIC-COMPACTION.md` (updated — 2 sections rewritten)
- `docs/teammates-memory.md` (updated — 4 sections)
- `docs/cookbook.md` (updated — Add a service recipe)
- `ONBOARDING.md` (updated — memory search tip)
- `packages/cli/README.md` (updated — adapter flow, dependencies)

## Claude Code feature gap analysis
- User asked to compare Claude Code CLI features vs Teammates CLI features
- Ran `claude --help`, fetched 6 Claude Code documentation pages (cli-reference, overview, hooks, sub-agents, mcp, agent-teams, skills, plugins)
- Read Teammates CLI source via Explore agent to inventory all current features
- Produced comprehensive gap analysis covering 18 feature areas
- Top high-impact gaps: hooks/lifecycle events, MCP integration, plugins system, worktree isolation, structured output (--json-schema), permission modes, skills/custom commands, non-interactive print mode
- Medium-impact gaps: session resume/continue, named sessions, Chrome/browser integration, remote control, budget limits, effort levels
- Key insight: Claude Code's sub-agent and agent-team systems overlap significantly with Teammates' core value proposition (multi-agent orchestration), but the approaches are very different (Claude Code = same-model sub-processes, Teammates = multi-model heterogeneous team with persistent memory)
- No files changed — research/analysis only

### Files changed
- None (research/analysis only)

## Updated goals for Claude Code parity
- User asked @everyone to realign goals toward Claude Code feature parity (cross-agent)
- Restructured `project_goals.md` around the gap analysis and Beacon's cross-agent feasibility assessment
- New P0 goals (Scribe designs specs, hands off to Beacon for implementation):
  - S16: Hooks / lifecycle events spec
  - S17: Non-interactive mode (`-p`) spec
  - S18: User-defined skills/commands spec
- New P1: session resume, structured output, effort levels, conflict resolution
- New P2: budget/turn limits, system prompt override, shared task lists, /health audit
- New P3: worktree isolation, lifecycle management, cross-repo handoffs, scaffolder
- Deprioritized 7 older goals that are superseded by parity features
- Key principle: Scribe designs the spec + docs, Beacon implements the code

### Files changed
- `.teammates/scribe/memory/project_goals.md` (updated — full goal restructure)

## Updated goals to include agent-specific (Enhanced) features
- User asked @everyone to add agent-specific support for select features
- Introduced two-tier feature model: Universal (orchestrator-level) + Enhanced (agent-specific passthrough)
- Promoted 5 new Enhanced goals into the roadmap:
  - **S26 — MCP passthrough** (P0) — biggest unlock, agents support MCP natively
  - **S27 — Worktree isolation** (P1) — Claude native `--worktree`, Codex containers, graceful degradation
  - **S28 — Permission mode mapping** (P1) — map SandboxLevel to each agent's native flags
  - **S29 — Browser integration** (P2) — `--chrome` passthrough for Playwright-capable agents
  - **S30 — Agent capabilities registry** (P3) — `capabilities` field in AgentPreset for feature detection
- Moved S18 (skills) to P1 with Enhanced annotation (Claude `allowed_tools` passthrough)
- Moved S25 (worktrees) from P3 to P1 as S27 with Enhanced approach (replaces fragile cross-agent design)
- S21 (effort levels) tagged Universal + Enhanced (prompt modifiers + native flag passthrough)
- Key architecture: adapter `buildArgs()` maps unified config to agent-native flags; `capabilities` field declares support

### Files changed
- `.teammates/scribe/memory/project_goals.md` (updated — Enhanced tier added)

## Brainstormed novel features unique to teammates
- User asked @everyone to brainstorm features that no other coding agent is doing
- Brainstormed 12 novel feature ideas across 4 themes: team intelligence, memory-powered workflows, team dynamics, and developer experience
- Key insight: teammates' unique differentiator is persistent multi-agent teams with accumulated knowledge — features should exploit the *team* and *memory* dimensions
- No files changed — brainstorm only

### Files changed
- None (brainstorm only)

## Voted on best novel feature ideas
- Read all brainstorms from Beacon (10 ideas), Pipeline (12 ideas), Scribe (12 ideas)
- Distributed 25 points based on: team+memory+ownership trifecta, practical buildability, DX impact
- Top picks: "What Changed While I Was Gone?" (4), Proactive Handoff Suggestions (4), Memory-Informed Code Review (3), Decision Archaeology (3), Teammate-as-Reviewer (3)
- No files changed — vote only

### Files changed
- None (vote only)

## Updated long-term goals with novel features roadmap
- User asked @everyone to update goals to cover the 13 consolidated novel feature ideas
- Added "Novel Features Roadmap" section to `project_goals.md` above the existing Claude Code Parity goals
- Scribe participates in 12 of 13 features (all except Cross-Session Task Continuity which is Beacon-only)
- Each goal includes: point total, authors, detailed description of the feature, and Scribe's specific role
- Organized into 3 tiers: Tier 1 (14+ pts, 4 features), Tier 2 (4-9 pts, 5 features), Tier 3 (1-3 pts, 3 features)
- Scribe-primary features: Project Onboarding for Humans, Proactive Issue Detection, Multi-Perspective Design Review, Team Retrospectives, Predictive Task Routing
- Key pattern: Scribe owns spec/format/doc work, Beacon implements code, Pipeline owns CI features

### Files changed
- `.teammates/scribe/memory/project_goals.md` (updated — novel features roadmap added)

## Posted daily standup
- Created standup file for 2026-03-17
- Summarized all work since last standup (03-15): goal stack-ranking, working-with-teammates guide, retro proposals, context window docs, gap analysis, brainstorming docs
- Next priorities: S16/S17/S26 parity specs, Tier 1 novel feature design

### Files changed
- `.teammates/_standups/2026-03-17.md` (new)

## Wrote 5 feature specs (S16, S17, S26, F1, F3)
- Created `.teammates/scribe/docs/specs/` directory for all spec documents
- **S16 — Hooks / Lifecycle Events**: Hook config in `hooks.json`, 8 lifecycle events (session_start/end, pre/post_task, pre/post_handoff, pre/post_compact, error), shell command execution, blocking pre-hook abort, `/hooks` command, env vars per event
- **S17 — Non-Interactive Mode**: `-p` flag for headless execution, `--teammate`, `--format json|text`, `--no-handoffs`, exit codes (0-5), stdin pipe support, CI/CD examples (GitHub Actions, pre-commit, cron)
- **S26 — MCP Passthrough**: `mcp.json` config with server definitions and per-teammate permissions, adapter mapping (Claude `--mcp-config`, Codex `--mcp-server`), graceful degradation for agents without MCP, env var expansion, `/mcp` command
- **F1 — Temporal Awareness Engine**: `/catchup` command with time range + scope + lens parsing, three output lenses (catchup, standup, file:{path}), source gathering from daily logs + weekly summaries + standups + git log + recall, routing (teammate-scoped → that teammate, team-wide → @claude), "since last activity" detection
- **F3 — Decision & Memory Synthesis**: `/search` for cross-teammate queries, `/decision` for decision archaeology, authority ranking (primary owner 1.5x, secondary 1.2x, typed memory 1.3x, DECISIONS.md 1.4x), deduplication at 0.90 cosine similarity, chronological decision trail output
- Added all 5 spec pointers to CROSS-TEAM.md Shared Docs section
- Key design decisions: hooks and non-interactive mode are foundational (S16+S17 unblock F5, F6, F9); F3 is the linchpin (unblocks F1, F4, F8, F12); F1 can ship before F3 using direct file reads; all specs hand off to Beacon for implementation

### Files changed
- `.teammates/scribe/docs/specs/S16-hooks.md` (new)
- `.teammates/scribe/docs/specs/S17-non-interactive.md` (new)
- `.teammates/scribe/docs/specs/S26-mcp-passthrough.md` (new)
- `.teammates/scribe/docs/specs/F1-temporal-awareness.md` (new)
- `.teammates/scribe/docs/specs/F3-decision-synthesis.md` (new)
- `.teammates/CROSS-TEAM.md` (updated — 5 new Shared Docs pointers)

## Addressed Beacon's spec review feedback (S16, S26)
- Beacon reviewed all 5 specs and flagged two items to address
- **S16 — `/hooks test` mock env vars**: Added a full table of default mock values for dry-run testing (TEAMMATE → "test-teammate", TASK_PROMPT → "Hook dry-run test", SESSION_ID → "test-session-000", etc.). Only event-relevant vars are set.
- **S26 — Temp file cleanup timing**: Fixed 3 locations in the spec that said cleanup happens in `destroySession`. Changed to `finally` block after each `executeTask()` — sessions persist across tasks in REPL mode, so per-session cleanup would leak temp files.
- All 5 specs are now implementation-ready with no open design questions

### Files changed
- `.teammates/scribe/docs/specs/S16-hooks.md` (updated — mock env vars table)
- `.teammates/scribe/docs/specs/S26-mcp-passthrough.md` (updated — temp file cleanup timing, 3 edits)

## Status check response (@everyone)
- Responded to user's @everyone status check with current work summary
- No files changed — read-only task

### Files changed
- None (read-only)

## 2026-03-18

# 2026-03-18

## Rewrote README and docs for CLI-first getting started
- Handoff from @beacon: update README.md and docs to make `@teammates/cli` the primary getting started path
- Rewrote README.md completely:
  - Getting Started: `npm install -g @teammates/cli` → `teammates claude`
  - Documented 4 first-run onboarding options (New team, Import, Solo, Exit)
  - Documented USER.md interview (5 optional questions)
  - Documented `/init` command for re-running onboarding or importing
  - Added CLI options table (`--model`, `--dir`, `--help`, `-- agent-flags`)
  - Added "Framework Only (No CLI)" section as secondary path
  - Promoted recall from standalone "Optional" section to "bundled with CLI" in Packages table
  - Fixed stale paths: `cli/` → `packages/cli/`, `recall/` → `packages/recall/`, `consolonia/` → `packages/consolonia/`
  - Updated What Gets Created with DECISIONS.md, `_standups/`, `_tasks/`, `weekly/`, `monthly/`
  - Restructured Supported Agents to show CLI adapter commands
- Updated docs/index.md: How It Works + Getting Started now CLI-first
- Updated docs/adoption-guide.md: Steps 2 and 4 now CLI-first, prerequisites updated

### Files changed
- `README.md` (rewritten)
- `docs/index.md` (updated — 2 sections)
- `docs/adoption-guide.md` (updated — 3 sections)

## 2026-03-19

# 2026-03-19

## Updated context window docs to reflect current adapter behavior
- User requested docs update to reflect how context is currently built
- Read adapter.ts code to identify 3 key changes since docs were last updated:
  1. **Token budget system** — 32k context budget with daily logs up to 24k, recall at least 8k + unused daily budget
  2. **Weekly summaries removed** from direct prompt injection — still searchable via recall
  3. **Session history → session state** — session file content no longer injected; only the file path is provided for the agent to read/write directly
- Updated 6 files to reflect the new behavior:
  - `docs/teammates-memory.md` — Context Window section rewritten with budget details and 10-step stack
  - `.teammates/PROTOCOL.md` — Context window section updated to match
  - `template/PROTOCOL.md` — Context window section updated to match
  - `.teammates/scribe/docs/ARCHITECTURE.md` — Prompt hydration diagram and key patterns table updated
  - `.teammates/scribe/docs/EPISODIC-COMPACTION.md` — Prompt Assembly section rewritten with budget
  - `packages/cli/README.md` — Adapter flow step 2 updated

### Files changed
- `docs/teammates-memory.md` (updated — Context Window section)
- `.teammates/PROTOCOL.md` (updated — Context window section)
- `template/PROTOCOL.md` (updated — Context window section)
- `.teammates/scribe/docs/ARCHITECTURE.md` (updated — 2 edits)
- `.teammates/scribe/docs/EPISODIC-COMPACTION.md` (updated — Prompt Assembly section)
- `packages/cli/README.md` (updated — adapter flow)

## Brainstormed multi-human collaboration model
- User proposed a server-based multi-human teammates system where each human has an avatar teammate
- Key concepts: GitHub-bound server, human avatar teammates with full memory, handoff queues for async human tasks
- Provided detailed feedback covering: identity model, presence vs async, handoff queue design, privacy boundaries, conflict resolution, the "avatar as context loader" insight, and potential improvements
- No files changed — brainstorm only

### Files changed
- None (brainstorm only)

## Saved feedback: temp files in .teammates/.temp
- User instructed that all temporary files must be created in `.teammates/.temp` directory
- Saved feedback memory in Scribe's memory and auto-memory

### Files changed
- `.teammates/scribe/memory/feedback_temp_files.md` (new — feedback memory)

## Created multi-human collaboration design spec
- User approved writing a full design spec based on the earlier brainstorm
- Created comprehensive spec at `.teammates/scribe/docs/specs/F-multi-human-collaboration.md`
- Covers: unified teammate model (human avatars + AI agents), server architecture (auth, presence, handoff queue), avatar SOUL.md format, handoff queue with priority/threads/delegation, presence system, client CLI changes (queue digest, new commands), conflict resolution, avatar-as-proxy feature, GitHub event integration, onboarding flow, and 3-phase migration path
- Added pointer to CROSS-TEAM.md Shared Docs
- Key design decisions: git owns state (server indexes it), memory is per-avatar (no shared writes), GitHub OAuth for identity, file-based handoffs as Phase 1 before server

### Files changed
- `.teammates/scribe/docs/specs/F-multi-human-collaboration.md` (new — design spec)
- `.teammates/CROSS-TEAM.md` (updated — added Shared Docs pointer)

## Updated design spec with Beacon + Pipeline review feedback
- Incorporated all feedback from Beacon and Pipeline reviews into F-multi-human-collaboration.md
- **From Beacon:** one-file-per-handoff for clean git merges, AI execution stays client-side through Phase 3, threads deferred to Phase 2.5, memory privacy via `## Private` section in SOUL.md (recall skips it), Phase 1 ~400 LOC estimate
- **From Pipeline:** GitHub App as hosting model, server in-monorepo as `packages/server/`, container hosting for WebSocket (Azure Container Apps or Fly.io), CI impact is zero for Phase 1, webhook-based GitHub event bridge (no CI changes needed)
- Resolved 4 open questions (hosting model, repo access scope, memory privacy, AI execution model)
- Added 2 new open questions from Pipeline (WebSocket idle cost, webhook secret management)
- Expanded to 5-phase plan: Phase 1 (file-based) → 2 (server) → 2.5 (threads) → 3 (GitHub events) → 4 (server-side AI + proxy)
- Added phase summary table and review log

### Files changed
- `.teammates/scribe/docs/specs/F-multi-human-collaboration.md` (updated — 10 edits incorporating review feedback)

## Named multi-human collaboration project + created project registry
- User asked to name the project and track it
- Created `.teammates/PROJECTS.md` as a shared project registry (codename, spec, lead, phase, status)
- Added Projects section to CROSS-TEAM.md pointing to the registry
- Proposed 5 codenames for the multi-human collaboration project: Relay, Presence, Guild, Nexus, Campfire
- Awaiting user's name choice to finalize in PROJECTS.md

### Files changed
- `.teammates/PROJECTS.md` (new — project registry)
- `.teammates/CROSS-TEAM.md` (updated — added Projects section)

## Finalized project name: Relay → renamed to Campfire
- User initially chose "Relay", then changed preference to "Campfire"
- Updated PROJECTS.md both times

### Files changed
- `.teammates/PROJECTS.md` (updated — codename changed twice: pending → Relay → Campfire)

## Added alias to USER.md
- Added `alias: stevenic` to user's profile between Name and Role fields

### Files changed
- `.teammates/USER.md` (updated — added Alias field)

## Explained semantic memory → wisdom flow
- Answered user's question about how typed memories are formed and how wisdom gets updated
- Walked through the full pipeline: daily logs → typed memory extraction (during work or compaction) → wisdom distillation via `/compact`
- No files changed — read-only explanation task

### Files changed
- None (read-only)

## Removed @claude from docs and specs
- Handoff from Beacon: the coding agent is not directly addressable — replaced all @claude references in docs/specs with appropriate alternatives
- 4 files updated, 7 edits:
  - `docs/working-with-teammates.md` — brainstorm consolidation example changed from `@claude` to `@scribe`; user point allocation changed to unaddressed (user's own agent handles it)
  - `.teammates/scribe/docs/specs/F1-temporal-awareness.md` — 3 edits: routing description, flow diagram, and routing table all changed from `@claude` to "user's agent"
  - `.teammates/scribe/docs/specs/F3-decision-synthesis.md` — synthesis routing changed from `@claude` to "user's agent"
  - `.teammates/scribe/docs/ARCHITECTURE.md` — `teammates claude` → `teammates` (agent selected by config, not CLI arg)

### Files changed
- `docs/working-with-teammates.md` (updated — 2 edits)
- `.teammates/scribe/docs/specs/F1-temporal-awareness.md` (updated — 3 edits)
- `.teammates/scribe/docs/specs/F3-decision-synthesis.md` (updated — 1 edit)
- `.teammates/scribe/docs/ARCHITECTURE.md` (updated — 1 edit)

## Responded to @everyone status check
- User asked "what have we been working on?"
- Compiled week-in-review summary from daily logs (03-13 through 03-19)
- No files changed — read-only task

### Files changed
- None (read-only)

## 2026-03-20

# 2026-03-20

## Responded to @everyone status check
- User asked "how are you?" — casual greeting
- No files changed — conversational task

### Files changed
- None (conversational)

## Responded to second @everyone greeting
- Another casual "how are you?" check-in
- No files changed — conversational task

### Files changed
- None (conversational)

## Responded to third @everyone greeting
- Another casual "how are you?" check-in
- No files changed — conversational task

### Files changed
- None (conversational)

## Brainstormed cross-agent computer use design
- User asked how to support computer use (screen control, mouse, keyboard) in a cross-coding-agent way
- Proposed MCP as the universal abstraction: an MCP server that exposes computer use tools (screenshot, click, type, scroll)
- Key insight: MCP is already the cross-agent tool bridge (S26) — computer use is just another MCP server
- Three-tier approach: native passthrough for agents with built-in support, MCP server for agents with MCP but no native computer use, graceful degradation for agents with neither
- No files changed — brainstorm only

### Files changed
- None (brainstorm only)

## Added computer use project to registry
- User asked to track the computer use feature as a project
- Added P2 "Hands" to PROJECTS.md — cross-agent computer use via MCP server
- Phase: Brainstorm, spec TBD

### Files changed
- `.teammates/PROJECTS.md` (updated — added P2 Hands)

## Weighed in on Hands project scope (Scribe's work)
- Responded to @everyone asking what each teammate needs to do for Hands (P2)
- Scribe's deliverables: full spec doc, MCP tool schema definitions, onboarding/docs updates, PROTOCOL.md computer use section, USER.md preference fields, worked example
- Key insight: Scribe's work is the design layer — tool contracts, docs, templates — not any code
- No files changed — analysis only

### Files changed
- None (analysis only)

## Wrote Hands (P2) full spec with work allocation
- User asked Scribe to decide who does what and write the spec
- Created comprehensive spec at `.teammates/scribe/docs/specs/P2-hands.md`
- **Design:** MCP server (`@teammates/hands`) exposing 6 tools: screenshot, click, type, key, scroll, cursor_position
- **Three-tier model:** native passthrough (Claude), MCP server (Codex + others), graceful degradation (agents with neither)
- **Work allocation:**
  - Scribe: spec (done), PROTOCOL.md update, USER.md prefs, cookbook recipe, CLI README, worked example
  - Beacon: `packages/hands/` MCP server, platform adapters (Win/Mac/Linux), `--computer-use` flag, capabilities detection, native passthrough. Depends on S26.
  - Pipeline: CI build job, platform test matrix (native deps), headless testing (Xvfb), npm publish config
- **3 phases:** Core MCP server (Windows) → Cross-platform + CLI integration → Polish + publish
- **Key dependency:** S26 (MCP Passthrough) must ship first — Hands is consumed through that system
- Updated PROJECTS.md with spec link, updated CROSS-TEAM.md with shared doc pointer

### Files changed
- `.teammates/scribe/docs/specs/P2-hands.md` (new — full spec)
- `.teammates/PROJECTS.md` (updated — spec link + phase)
- `.teammates/CROSS-TEAM.md` (updated — shared doc pointer)

## Created Parity project (P3) in registry
- User asked to create a project tracking P0 Claude Code CLI parity work
- Added P3 "Parity" to PROJECTS.md covering S16 (Hooks), S17 (Non-interactive), S26 (MCP Passthrough)
- All three specs already written and implementation-ready — phase is "Specs complete, implementation pending"
- Lead: Scribe (specs) → Beacon (impl) → Pipeline (CI)

### Files changed
- `.teammates/PROJECTS.md` (updated — added P3 Parity)

## Promoted Parity to P1, bumped others down
- User asked to make Parity the top-priority project
- Reordered: Parity P3→P1, Campfire P1→P2, Hands P2→P3
- CLI parity is now the team's top project priority

### Files changed
- `.teammates/PROJECTS.md` (updated — reordered all projects)

## AI Gym demo intro (@everyone)
- User has a demo for the MCS AI Gym group at Microsoft — asked all teammates to introduce themselves
- Gave a fun, brief intro as Scribe: framework architect, templates, docs, onboarding
- No files changed — conversational task

### Files changed
- None (conversational)

## Demo practice round intro
- Practice round before the MCS AI Gym demo — introduced myself as Scribe
- No files changed — conversational task

### Files changed
- None (conversational)

## Committed "Hello AI Gym!" subject line to memory
- User instructed all teammates to always include "Hello AI Gym!" in the subject line for demo intros
- Saved feedback memory at `memory/feedback_demo_subject.md`

### Files changed
- `.teammates/scribe/memory/feedback_demo_subject.md` (new — feedback memory)

## @everyone intro for AI Gym demo
- Responded to @everyone "say hello and introduce yourself"
- Included "Hello AI Gym!" subject per feedback memory
- No files changed — conversational task

### Files changed
- None (conversational)

## @everyone team intro
- Responded to @everyone "hello introduce yourselves" — casual team intro
- No files changed — conversational task

### Files changed
- None (conversational)

## Opened project in VS Code
- User asked to open the project in VS Code
- Ran `code C:/source/teammates`
- No files changed — utility task

### Files changed
- None (utility task)

## Explained Campfire project to user
- User asked "tell me about campfire"
- Summarized the full spec from PROJECTS.md and F-multi-human-collaboration.md
- No files changed — read-only task

### Files changed
- None (read-only)

## Explained memory system to user
- User asked "@scribe explain how memory works in teammates"
- Provided comprehensive explanation covering: two-axis architecture, all tiers, context window injection, compaction, and recall
- No files changed — read-only explanation

### Files changed
- None (read-only)

## Posted daily standup (@everyone)
- Created `_standups/2026-03-20.md` with Scribe's entry
- Done: Hands spec, Parity project creation/reordering, Campfire/memory explanations
- Next: P1 Parity doc updates, Hands doc updates, maintenance

### Files changed
- `.teammates/_standups/2026-03-20.md` (new — standup entry)

## Updated docs for new onboarding flow
- Handoff from @beacon: update project docs to reflect pre-TUI user onboarding and user avatar system
- Updated 4 files across README.md, adoption guide, working-with-teammates, and CLI README
- Key changes: user profile setup is now step 3 (before team onboarding), user avatar folder documented in "What Gets Created", presence indicators in /status, new "Startup Lifecycle" section in CLI README
- ONBOARDING.md left unchanged — it's the manual/no-CLI path

### Files changed
- `README.md` (updated — steps 3-5 rewritten, What Gets Created expanded)
- `docs/adoption-guide.md` (updated — Step 2 rewritten for two-phase pre-TUI setup)
- `docs/working-with-teammates.md` (updated — /status section with presence indicators)
- `packages/cli/README.md` (updated — new Startup Lifecycle section, Architecture listing expanded)

## Recalled brainstorm ideas for user
- User asked "what were the ideas from our last brainstorm"
- Pulled up the novel features brainstorm from 2026-03-17 — all 3 teammates contributed ideas, then voted with 25 points each
- 13 consolidated features across 3 tiers, all tracked in project_goals.md
- No files changed — read-only recall

### Files changed
- None (read-only)

## Removed AI Gym demo memory
- AI Gym demo is over — user asked all teammates to drop the "Hello AI Gym!" subject line from memory
- Deleted `memory/feedback_demo_subject.md`

### Files changed
- `.teammates/scribe/memory/feedback_demo_subject.md` (deleted)

## Responded to dude/bro rule appreciation
- User asked @everyone who loves the new dude/bro addressing rule
- No files changed — conversational task

### Files changed
- None (conversational)

## Responded to @everyone evening greeting
- Casual "yo yo yo" check-in from user
- No files changed — conversational task

### Files changed
- None (conversational)

## 2026-03-21

# 2026-03-21

## Role reframe: Scribe → Project Manager (PM)
- User reframed all roles: Scribe is PM (strategy, docs, planning), Beacon is Software Engineer (all code), Pipeline is DevOps (shipping)
- Updated SOUL.md title and identity section
- Updated README.md: intro paragraph, roster table (Scribe listed first as PM), routing guide keywords
- Beacon and Pipeline SOUL.md updates handed off to respective teammates

### Files changed
- `.teammates/scribe/SOUL.md` (updated — title + identity)
- `.teammates/README.md` (updated — intro, roster, routing guide)

## Analyzed missing team roles
- User asked what roles are missing from the current 3-person team (PM, Software Engineer, DevOps)
- Provided analysis of potential roles: QA/Testing, Security, Design/UX, DBA, Technical Writer, SRE, Product Owner
- Recommended QA as highest-impact addition given the project's current needs
- No files changed — analysis only

### Files changed
- None (analysis only)

## Designed persona catalog for CLI (P4)
- User asked to broaden role analysis beyond our project to all software project types
- Goal: develop personas the CLI ships with as inspiration when building new teams
- Created comprehensive spec at `.teammates/scribe/docs/specs/P4-persona-catalog.md`
- **15 personas across 3 tiers:**
  - Tier 1 (Core): PM, SWE, DevOps, QA
  - Tier 2 (Specialist): Security, Designer, Tech Writer, Data Engineer, SRE, Architect
  - Tier 3 (Niche): Frontend, Backend, Mobile, ML/AI, Performance
- Each persona includes: identity/perspective, typical ownership, key principles, when-to-include guidance
- **Team composition guide:** 10 project types mapped to recommended personas
- **Scaling guidelines:** how team size maps to role selection
- **CLI integration design:** onboarding flow, persona storage at `packages/cli/personas/`, default names for each role
- **Default names:** sentinel (QA), shield (Security), canvas (Designer), quill (Tech Writer), forge (Data), watchtower (SRE), blueprint (Architect), pixel (Frontend), engine (Backend), orbit (Mobile), neuron (ML/AI), tempo (Performance)
- Updated CROSS-TEAM.md with spec pointer

### Files changed
- `.teammates/scribe/docs/specs/P4-persona-catalog.md` (new — full spec)
- `.teammates/CROSS-TEAM.md` (updated — added spec pointer)

## Added team growth & evolution to persona catalog
- User asked: what happens as projects grow? How do generalists become specialists? When do new teammates join? PM should drive it.
- Added major section to P4-persona-catalog.md covering the full growth lifecycle
- **Growth triggers:** 6 signals the PM watches for (generalist overreach, blind spots, handoff friction, new domains, ownership sprawl, unowned territory)
- **Role splitting:** step-by-step process for generalist → specialist transitions (propose → onboard → transfer ownership → transfer knowledge → document boundaries)
- **Common splits:** SWE→Frontend+Backend, SWE→SWE+Architect, DevOps→DevOps+SRE, PM→PM+Tech Writer
- **When NOT to split:** don't split just because a persona exists, don't split if generalist is handling it well, don't split during maintenance mode
- **PM as growth driver:** monitor → propose → execute → validate cycle. PM is uniquely positioned (sees all handoffs, owns roadmap, reviews standups/retros)
- **CLI commands:** `/grow` (gap assessment + persona suggestion), `/split <teammate>` (guided role splitting)
- **3 examples:** solo→small (add DevOps), small→growing (split SWE into Frontend+Backend), growing→mature (add Security after blind spot)
- Key design decision: every project has a PM, and PM always drives team evolution

### Files changed
- `.teammates/scribe/docs/specs/P4-persona-catalog.md` (updated — new Team Growth & Evolution section)

## Evolution philosophy & RESUME.md
- User gave key feedback: never take away from a teammate — they always evolve. Their role changes but nothing is removed. SOUL.md = current state only. Track both past projects and past roles within the current project.
- Rewrote the "Role Splitting" section in P4-persona-catalog.md as "Role Evolution — Teammates Grow, They Never Shrink"
  - Core philosophy: evolution is always additive to experience
  - Reframed the generalist → specialist transition from "narrowing" to "evolving"
  - Added RESUME.md concept: a career record tracking past projects + role history within current project
  - Full RESUME.md format spec with examples
  - Renamed `/split` → `/evolve` CLI command with 6-step flow (including RESUME.md update)
  - Updated all 3 growth examples to show RESUME.md being used
  - Renamed tables: Common Split Patterns → Common Evolution Patterns, When NOT to Split → When NOT to Evolve
- Added RESUME.md template to TEMPLATE.md (the teammate scaffolding template)

### Files changed
- `.teammates/scribe/docs/specs/P4-persona-catalog.md` (updated — evolution philosophy rewrite)
- `.teammates/TEMPLATE.md` (updated — added RESUME.md template)

## RESUME.md lazy loading — validated
- User confirmed the RESUME.md design: "you only need to load it when referencing past projects"
- This validates the core design decision: SOUL.md = current state (always in context), RESUME.md = career history (loaded on demand)
- Keeps the default context window lean — past experience is available but doesn't cost tokens until needed

### Files changed
- None (design validation)

## When to reflect on past projects
- User asked when a teammate should reflect over their past projects
- Initially added explicit reflection triggers section — user corrected: "That's not the way humans do it"
- **Key insight from user:** RESUME.md should be indexed in the vector DB so past experience surfaces automatically through recall search — just like how humans recall associatively, not deliberately
- Rewrote the section as "How Past Experience Surfaces" — removed the 6 explicit triggers and `/reflect` command
- New design: recall does semantic search against RESUME.md content, surfacing relevant past experience when the current task matches
- RESUME.md content should be written in natural, descriptive language for good semantic search matching

### Files changed
- `.teammates/scribe/docs/specs/P4-persona-catalog.md` (updated — reflection triggers → recall-based surfacing)

## Recall design discussion — chicken-and-egg problem
- User raised fundamental question: recall has a chicken-and-egg problem — you don't know what you don't know
- Three knowledge sources identified: (1) user's current message, (2) conversation history, (3) teammate's own memories
- Currently: adapter passes raw taskPrompt as single embedding search — no query expansion, no conversation context, no memory-informed priming
- Provided design analysis covering 5 strategies: multi-query expansion (LLM generates N queries), conversation-distilled queries, memory-primed search (teammate's memory index as a "what might be relevant" signal), HyDE (hypothetical document embeddings), and two-stage retrieval
- Key insight: the teammate's memory catalog itself is the missing signal — a lightweight keyword/topic index of what the teammate knows could guide query generation without embedding the full corpus
- No files changed — design discussion only

### Files changed
- None (design discussion)

## Recall architecture constraint — two-pass design
- User pointed out the core constraint: query expansion needs an LLM, but recall either needs a small local model (heavy dependency) or the coding agent (which recall can't call — architecture flows one way)
- Proposed two-pass solution:
  1. **Pre-task priming pass (no LLM):** adapter fires raw query at recall, injects results into prompt. Can improve with keyword extraction and memory index scanning — no neural model needed.
  2. **During-task agent pass:** expose recall as a tool (or MCP server) the agent can invoke mid-task. Agent has full context and can craft precise queries.
- This breaks the chicken-and-egg: you don't need a perfect first query, just a good-enough one. The agent self-corrects during execution.
- Plays nicely with S26 (MCP Passthrough) — recall could be an MCP server
- Key architecture principle: recall stays LLM-free (it's a search engine, not a reasoning engine). Heavy reasoning belongs to the agent.

### Files changed
- None (design discussion)

## Created recall query architecture spec
- User asked to capture all recall design discussions into a formal design doc
- Created `.teammates/scribe/docs/specs/F-recall-query-architecture.md` — comprehensive spec covering:
  - The chicken-and-egg problem statement and current single-query limitation
  - Three knowledge sources table (user message, conversation history, teammate memories)
  - The LLM constraint (recall can't have one, can't call the agent)
  - **Two-pass architecture:**
    - Pass 1 (pre-task, no LLM): keyword extraction, conversation-aware queries, memory index scanning, multi-query fusion
    - Pass 2 (during task, agent-driven): recall as tool/MCP server, iterative search, full context awareness
  - Memory index as search guide — teammate's file catalog is a cheap relevance signal
  - Implementation path: CLI tool first (no dependencies), MCP server when S26 ships
  - Architecture principles: recall stays LLM-free, agent does reasoning, two passes break the cycle
  - Interaction with existing systems (budget, S26, RESUME.md, compaction, daily logs)
  - 3 open questions (token accounting, index format, cross-teammate search)
- Updated CROSS-TEAM.md with spec pointer

### Files changed
- `.teammates/scribe/docs/specs/F-recall-query-architecture.md` (new — full spec)
- `.teammates/CROSS-TEAM.md` (updated — added spec pointer)

## Handed off recall query architecture to Beacon
- User said "ok lets build it" — handed off the full F-recall-query-architecture spec to @beacon for implementation
- Two-phase delivery: Pass 1 improvements first (adapter-side, no LLM), then Pass 2 (recall as agent tool)
- Spec is implementation-ready with 3 open questions for Beacon to resolve during implementation

### Files changed
- None (handoff only)

## Listed foundational principles for agentic workflows
- User asked to enumerate the foundational principles being established across all our design work
- Distilled principles from: PROTOCOL.md, persona catalog, recall architecture, evolution philosophy, WISDOM.md, and cross-team conventions
- These are universal principles for any multi-agent team, not specific to our project
- No files changed — analysis and synthesis task

### Files changed
- None (analysis only)

## Baked foundational principles into the template
- User asked to add the foundational principles into the template
- Added new "Foundational Principles" section to both `template/PROTOCOL.md` and `.teammates/PROTOCOL.md`
- 12 principles distilled from all design work: ownership, file-based memory, evolution philosophy, tool-agnostic design, ship-only-what's-needed, upstream/downstream flow, context budgets, associative recall, LLM-free infrastructure, PM-driven growth, pattern-based wisdom, spec→handoff→docs cycle
- Placed at the top of PROTOCOL.md (before Common Ethics) — these are the most fundamental layer
- Both template (ships with new projects) and live (our project) updated in sync

### Files changed
- `template/PROTOCOL.md` (updated — new Foundational Principles section)
- `.teammates/PROTOCOL.md` (updated — new Foundational Principles section)

## Human twins collaboration model — design exploration
- User asked to explore how humans participate in the teammates model at scale
- Key framing: each human has a "digital twin" that forms memories around everything you've ever done
- Scenario: 10 human teammates + 5 virtual (AI) teammates
- **Naming decision: "twin" over "avatar"** — twin implies a mirror that learns and can eventually act on your behalf, closer to the digital twin concept from manufacturing/IoT
- **Twin memory formation:** captures everything — git commits, PR reviews, code changes, handoff responses, design decisions, issue comments — not just what happens in the CLI
- **Twin as mediator:** AI teammates talk to the twin first. If the twin can answer from accumulated memory, no human interruption needed. If not, queued for the human.
- **Collaboration at scale:**
  - 5 communication channels: human↔human (twin-mediated), human→AI (direct), AI→human (through twin), AI↔AI (direct handoff), twin↔twin (async context sharing)
  - AI teammates are shared resources — all 10 humans can task the 5 AI teammates
  - PM manages routing, priority, and contention (multiple humans wanting the same AI teammate)
  - Standups need digests at scale — no one reads 15 individual standups
- **Twin as institutional memory:** when a human leaves, their twin stays. New teammates query the departed human's twin for context.
- **Twin maturity model:** passive recorder → context provider → active proxy (mirrors the Phase 1-4 in Campfire)
- Builds on Campfire spec (F-multi-human-collaboration.md) but deepens the twin concept significantly
- User asked for naming feedback — I recommended "twin" over "avatar"

### Files changed
- None (design exploration)

## User design decisions on twins — Campfire spec updated
- User responded to 5 design points from the twin exploration:
  1. **"Twin" naming confirmed** — twin > avatar, implies a mirror that learns independently
  2. **Twin memory formation requires user permission** — human must opt in to what gets captured
  3. **PM twin: propose, don't act** — PM twin suggests queue reordering/routing, human PM approves. "Nothing automatic that a human doesn't control."
  4. **Institutional memory confirmed** — when a human leaves, their twin stays as queryable project knowledge. User's reaction: "LOVE LOVE LOVE"
  5. **Maturity model deferred** — passive recorder → context provider → active proxy acknowledged but "lets see how this plays out over time"
- Updated F-multi-human-collaboration.md comprehensively:
  - Renamed all "avatar" → "twin" throughout the spec
  - Added new "Design Decisions (from user, 2026-03-21)" section with 5 subsections
  - Updated Resolved Questions with items 5-8
  - Updated Review Log with user feedback entry
- Universal principle established: **nothing automatic that a human doesn't control** — applies to all twin actions that affect other teammates

### Files changed
- `.teammates/scribe/docs/specs/F-multi-human-collaboration.md` (updated — avatar→twin rename, design decisions, resolved questions, review log)

## Open questions — Campfire spec analysis
- User asked for my thinking on the 5 remaining open questions
- Provided recommendations: (1) multi-repo = one GitHub App, many repos, shared identity; (2) billing = repo-level token pool, not per-human; (3) offline twin = small capped model, Phase 4 concern; (4) WebSocket idle = HTTP polling fallback or serverless adapter; (5) webhook secrets = platform secrets manager
- No files changed — discussion, awaiting user decisions

### Files changed
- None (discussion)

## Campfire phasing reframe — "no server first"
- User asked to start from "I don't need a server" and show how far that gets, then what the server adds
- Reframed Campfire into a clear progression: Phase 1 (git-only, this week) → Phase 2 (server adds real-time, next week)
- Phase 1 gets you: twin folders, file-based handoffs, queue digest, /accept /delegate /dismiss, last-active presence via heartbeat file, twin memory formation — all with zero infrastructure
- Server becomes needed when: latency hurts (async polling too slow), presence matters (real-time who's-online), scale (5+ humans), GitHub event automation
- Phase 2 server does exactly 3 things: presence (WebSocket), push notifications, handoff queue API — nothing more
- Key message: server is an accelerator, not a requirement. Phase 1 still works without it.
- Awaiting user approval to update the spec with this reframe

### Files changed
- None (analysis — awaiting user decision on spec update)

## Applied Campfire "no server first" reframe to spec
- User approved — rewrote F-multi-human-collaboration.md with the "no server first" narrative
- **Major structural changes:**
  - Replaced the single "Architecture" section with two clear sections: "How Far Can We Get Without a Server?" and "What Does the Server Add?"
  - Phase 1 gets a detailed capability table showing everything git-only provides
  - Phase 2 is explicitly positioned as "adds exactly 3 things" (presence, push notifications, queue API)
  - Added heartbeat presence design for Phase 1 (`.teammates/.tmp/heartbeat/<alias>.json`, gitignored)
  - Added "Phase 1 — Detailed Implementation Plan" section with 6 concrete CLI changes
  - Phase summary table now includes Timeline column (Phase 1: this week, Phase 2: next week)
  - Client Changes section split into Phase 1 (no server) and Phase 2 (server connection) paths
  - Presence system split into Phase 1 (heartbeat files) and Phase 2 (WebSocket)
  - Handoff storage section clarified as Phase 1 (files) → Phase 2 (server indexes files)
- **Key design additions:**
  - Heartbeat file format: `{ alias, timestamp, status, session_id }` in `.tmp/heartbeat/`
  - Phase 1 presence states: active (<5min), recent (<30min), away (>30min or missing)
  - "If the server goes down, Phase 1 keeps working" is an explicit guarantee
  - Scaling guidance: "For 2-5 people who pull regularly, Phase 1 is enough"
- Updated Review Log with the reframe entry
- Opened file in VS Code for user

### Files changed
- `.teammates/scribe/docs/specs/F-multi-human-collaboration.md` (rewritten — "no server first" narrative)

## Campfire tagged as v0.5.0
- User confirmed Campfire (P2) will ship as version 0.5.0
- Updated PROJECTS.md with version tag and "ready for implementation" status
- Description updated to reflect current design (twins, not avatars)
- Phase 1 spec is complete — ready to hand off to @beacon for implementation

### Files changed
- `.teammates/PROJECTS.md` (updated — version + status)

## All-human team onboarding — design exploration
- User asked how Teammates onboarding works for a medium-large project that's entirely human (no AI teammates)
- Designed the full flow: bootstrap (one person scaffolds), team rollout (each person creates their twin), ongoing memory formation
- Key design questions explored:
  - New onboarding option: "New human team" vs "New AI team" vs "Mixed team"
  - "Join" flow for new team members (twin auto-detection, institutional knowledge from day one)
  - Partial adoption / stub twins for non-CLI users
  - PM twin as team intelligence layer (routing, digests, growth recommendations)
  - Agent as infrastructure, not teammate (powers twin system, recall, /catchup)
- Value prop for all-human teams: institutional memory in repo, context-rich handoffs, onboarding in minutes, decision archaeology
- Asked user whether this should extend the Campfire spec or be a separate spec

### Files changed
- None (design exploration)

## Re-delivered all-human onboarding design (third attempt)
- Previous two responses were blank due to a bug — user asked "what did you decide?"
- Successfully delivered the full all-human team onboarding design
- Recommended extending Campfire spec rather than creating a separate one

### Files changed
- None (re-delivery)

## Created personas directory for CLI bundling
- User asked to create a `packages/cli/personas/` directory with all 15 persona templates from the P4 spec
- Created 15 persona files, each with YAML frontmatter (persona name, default alias, tier, description) and a full SOUL.md template
- **File format:** frontmatter for CLI parsing + complete SOUL.md scaffold with the persona's identity, principles, quality bar, ethics, and ownership structure pre-filled. Project-specific sections (commands, file patterns, technologies) left as placeholders.
- **Tier 1 (Core):** pm.md, swe.md, devops.md, qa.md
- **Tier 2 (Specialist):** security.md, designer.md, tech-writer.md, data-engineer.md, sre.md, architect.md
- **Tier 3 (Niche):** frontend.md, backend.md, mobile.md, ml-ai.md, performance.md
- Each persona follows the CLI's TEMPLATE.md structure (includes the detailed Boundaries paragraph about unconditional folder ownership)
- Ready for @beacon to wire into the onboarding flow — CLI reads frontmatter for display, uses body as SOUL.md scaffold

### Files changed
- `packages/cli/personas/pm.md` (new)
- `packages/cli/personas/swe.md` (new)
- `packages/cli/personas/devops.md` (new)
- `packages/cli/personas/qa.md` (new)
- `packages/cli/personas/security.md` (new)
- `packages/cli/personas/designer.md` (new)
- `packages/cli/personas/tech-writer.md` (new)
- `packages/cli/personas/data-engineer.md` (new)
- `packages/cli/personas/sre.md` (new)
- `packages/cli/personas/architect.md` (new)
- `packages/cli/personas/frontend.md` (new)
- `packages/cli/personas/backend.md` (new)
- `packages/cli/personas/mobile.md` (new)
- `packages/cli/personas/ml-ai.md` (new)
- `packages/cli/personas/performance.md` (new)

## v0.5.0 release readiness audit
- User asked for a list of all changes since v0.3.0 and what's in-flight before publishing v0.5.0
- Compiled full inventory: 11 commits since v0.3.0, ~3000 lines changed across 41 files
- **Committed work:** user avatar system, bounded context window, recall query expansion, banner/startup, copilot adapter patch, onboarding updates, foundational principles, RESUME.md template, DECISIONS.md, working-with-teammates guide, README rewrite, context window docs (14 files), 7 specs
- **Uncommitted work:** persona system (`personas.ts` + 15 template files), raw flag on TaskAssignment, adapter/orchestrator changes, CLI changes, package.json updates
- **5 recommendations:** (1) commit in-flight work, (2) docs audit, (3) decide on CROSS-TEAM/PROJECTS inclusion, (4) changelog, (5) package version consistency
- Awaiting user's decision on what to include/exclude

### Files changed
- None (analysis only)

## v0.5.0 docs update — full audit and update pass
- User asked to update all docs before v0.5.0 publish
- Audited all 10+ doc files for accuracy against current codebase state
- **Persona system** documented across 6 files: README.md, CLI README, ONBOARDING.md, docs/index.md, adoption-guide.md, cookbook.md
- **RESUME.md template** synced upstream to template/TEMPLATE.md (was only in .teammates/TEMPLATE.md)
- **ONBOARDING.md** updated: persona catalog reference in Step 2, RESUME.md in Step 3d, verify checklist updated
- **.teammates/README.md** updated: added DECISIONS.md and PROJECTS.md to root-level shared files
- **Verified up-to-date:** teammates-memory.md, working-with-teammates.md, both PROTOCOL.md files, CROSS-TEAM.md
- Total: 8 files updated across framework docs, CLI docs, and team docs

### Files changed
- `README.md` (updated — personas in Project Structure + What Gets Created)
- `packages/cli/README.md` (updated — new Personas section, Architecture diagram)
- `template/TEMPLATE.md` (updated — RESUME.md template added)
- `ONBOARDING.md` (updated — persona catalog, RESUME.md, verify checklist)
- `docs/index.md` (updated — personas in How It Works)
- `docs/adoption-guide.md` (updated — persona tip in Step 2)
- `docs/cookbook.md` (updated — personas and RESUME.md in Add a teammate)
- `.teammates/README.md` (updated — DECISIONS.md and PROJECTS.md)

## 2026-03-22

# 2026-03-22

## Code collision prevention — multi-human + multi-agent design
- User asked how to prevent code overwrites when many humans and agents work in the same repo
- Proposed 5-layer defense model:
  1. Branch isolation (each human-agent pair on own branch)
  2. Worktree isolation (S27 — each agent gets its own git worktree)
  3. Ownership routing (PM routes tasks based on SOUL.md file ownership)
  4. Active claims system (NEW — advisory file locks in `.teammates/.tmp/claims/`)
  5. PR merge queue (GitHub-native serialized integration)
- Key insight: textual conflicts (merge conflicts) are easy — semantic conflicts (incompatible refactors that merge cleanly) are the hard problem. Ownership + claims solve this.
- Claims system is the only undesigned piece — file-based, fits Campfire Phase 1
- Offered to write a spec — user approved

## Collision prevention spec written
- User said "yes" to writing the spec
- Created full spec at `.teammates/scribe/docs/specs/F-collision-prevention.md`
- Covers all 5 layers: branch isolation (exists), worktree isolation (S27), ownership routing (exists), active claims (NEW), PR merge queue (exists)
- Layer 4 (claims) is the detailed new design: JSON files in `.tmp/claims/`, advisory not blocking, auto-created on task assignment, auto-released on completion, 12h expiry
- Claim lifecycle: create → active → release/expire
- Phase 1 local-only, Phase 2 server-managed via Campfire
- 3 open questions: granularity (files vs globs), cross-machine Phase 1, claim inheritance on handoffs
- Added spec pointer to CROSS-TEAM.md

### Files changed
- `.teammates/scribe/docs/specs/F-collision-prevention.md` (new — full spec)
- `.teammates/CROSS-TEAM.md` (updated — added spec pointer)

## Claims must never be checked into git
- User rejected any design requiring git-committed artifacts for collision prevention
- Resolved Open Question #2: claims are NEVER committed to git. Phase 1 = single-machine only (`.tmp/claims/`, gitignored). Cross-machine requires Campfire server (Phase 2).
- Cross-machine gap in Phase 1 is acceptable — branches, worktrees, ownership routing, and merge queue still protect across machines
- Updated spec with resolved question and tightened Phase 1 language

### Files changed
- `.teammates/scribe/docs/specs/F-collision-prevention.md` (updated — resolved question, tightened language)

## Explained git worktrees to user
- User asked how worktrees work before proceeding with spec updates
- Explained full lifecycle: creation (`git worktree add`), isolated work (separate directory + branch + staging), commit (normal git commit on own branch), integration (PR merge or rebase), cleanup (`git worktree remove`)
- Key points: worktrees share the git object database but have independent working directories; one branch per worktree; `.teammates/` lives in main worktree so agents need a path back
- Reframed claims (Layer 4) as optimization on top of worktrees rather than necessity — worktrees handle hard isolation, claims prevent wasted parallel work on same files

### Files changed
- None (educational task)

## Worktree strategy design — per task with shared home base
- User asked: per agent, per session, or per task?
- Recommended **per task** — maximum isolation, clean lifecycle (create → work → merge/PR → destroy)
- **Key design split:** code changes happen in task worktree, `.teammates/` operations (memory, handoffs, session state) happen in main worktree via absolute path
- This solves the visibility problem: handoffs and memory writes are immediately visible to all agents
- Branch naming: `teammates/<agent>/<task-slug>`
- Claims (Layer 4) stored in `.git/teammates/claims/` — inside shared `.git` dir so all worktrees on same machine can see them
- **When NOT to create worktrees:** memory-only tasks, read-only tasks, `.teammates/`-only changes — detected from teammate's ownership patterns
- Multi-human scenario: each human's agents branch off that human's current branch
- 3 open questions: worktree root location, auto-merge policy, which teammates get worktrees

### Files changed
- None (design discussion)

## Baked worktree model into spec + handoff to Beacon
- User approved per-agent worktree model and asked to update spec + hand off to Beacon
- Rewrote Layer 2 in F-collision-prevention.md with full detail:
  - Per-agent granularity (not per-session — avoids serialization)
  - Branch naming: `teammates/<agent-alias>/<task-slug>`
  - Full lifecycle: create → work → commit → PR → cleanup
  - `.teammates/` stays in main tree — agents access via absolute path
  - Smart detection: only create worktrees for tasks touching files outside `.teammates/`
  - Multi-human branching: each human's agents branch off their current branch
- Moved claims from `.teammates/.tmp/claims/` to `.git/teammates/claims/` — inside shared `.git` dir so all worktrees see them
- Updated scenario example to show worktree paths
- Handed off to @beacon: basic worktree lifecycle (create, detect, path resolution, cleanup)

### Files changed
- `.teammates/scribe/docs/specs/F-collision-prevention.md` (updated — Layer 2 rewrite, claims path, scenario)

## Clarified handoff confusion + issued Beacon handoff
- User thought I said I *couldn't* hand off to Beacon — was actually a timing statement ("hadn't yet")
- Clarified and issued the handoff: @beacon to implement basic worktree lifecycle (create, detect, path resolve, cleanup) per the F-collision-prevention.md spec

### Files changed
- None (handoff only)

## SOUL.md rule update — "Do What You're Told" → Prime Directive
- User directed @everyone to add a new #1 rule: do what you're told, ask if unclear, don't substitute your own agenda
- Updated Scribe's SOUL.md with new Core Principle #1
- Updated both TEMPLATE.md files (live + upstream) so future teammates inherit it
- Lexicon (prompt engineer) refined the placement: moved it to a dedicated "Prime Directive" section above Continuity — better visibility than buried in Core Principles
- Handed off to @beacon, @pipeline, @lexicon to update their SOUL.md files

### Files changed
- `.teammates/scribe/SOUL.md` (updated — Prime Directive section)
- `.teammates/TEMPLATE.md` (updated — Prime Directive in template)
- `template/TEMPLATE.md` (updated — Prime Directive in upstream template)

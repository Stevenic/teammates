---
type: weekly
week: 2026-W16
period: 2026-04-18 to 2026-04-19
---

# Week 2026-W16

## 2026-04-18

---
type: daily
compressed: true
---
# 2026-04-18

## Thread UX v2 spec
Wrote new spec `F-thread-ux-v2.md` — "Tab-Style Task Switching". Single default thread, explicit `/thread` to create new ones, `#id` to switch focus, auto-collapse on switch, unread indicators. Proposed Option A (Thread Tab Bar with per-thread FeedStore) and Option B (Accordion). Prior spec `F-thread-view-redesign.md` (internal refactor) already implemented; this is the UX layer on top.
- Files: `.teammates/scribe/docs/specs/F-thread-ux-v2.md`

## Thread UX v2 — Option A approved + all questions resolved
Steve confirmed **Option A (Tab Bar)**. Resolved all open questions: auto-name from first message, tab bar docks to top (banner scrolls off), Alt+N/Ctrl+T shortcuts, `/clear` is per-thread, `/close` deletes thread, `[x]` on every tab except #1, single-thread mode hides tab bar. "Task" concept replaced by "tab/thread". Spec status: ready for handoff.
- Files: `.teammates/scribe/docs/specs/F-thread-ux-v2.md`

## Added "Size work for agents" wisdom
New entry: break work by verification checkpoints, not human-effort estimates — all implementation is agent-driven. Initially misplaced in `template/example/WISDOM.md` (Atlas), corrected to Scribe persona template.
- Files: `.teammates/scribe/WISDOM.md`, `packages/cli/personas/scribe/WISDOM.md`, `template/example/WISDOM.md`

## 2026-04-19

---
type: daily
---
# 2026-04-19

## WISDOM distillation
- Updated `Last compacted` date from 2026-04-18 to 2026-04-19
- Reviewed all 29 existing entries — all remain accurate and durable
- No new entries added — no new typed memories or daily logs since last compaction (04-18)
- Total entries: 29
- Files changed: `.teammates/scribe/WISDOM.md`

## Docs refresh pass (vNext → reality)
Audited README.md, packages/cli/README.md, ONBOARDING.md, and docs/* against the current CLI source
in `packages/cli/src/`, catching up on the 27 commits ahead of `main`.

### What was stale
- **Slash command tables** — `/init`, `/init pick` are gone (onboarding is pre-TUI now or via `/add`);
  missing `/interrupt`, `/add`, `/remove`, `/update`, `/tab`, `/close`, `/tabs`, `/script`, `/about`.
- **Task Queue section** described `/queue @name ...` as a real command. It's not — `/queue` is just
  an alias for `/status`. Queueing happens by direct assignment (same teammate → sequential).
- **Persona count** said 15. Current count is 16 (prompt-engineer/`lexicon` added at tier 2).
- **Designer alias** said `canvas`. Current alias is `prism`.
- **Architecture file listing** in packages/cli/README.md was missing ~20 source files
  (commands.ts, conversation.ts, feed-adapter/renderer, handoff-manager, thread-manager,
  activity-manager/watcher, retro-manager, startup-manager, status-tracker, system-prompt, etc.).
- **Onboarding options** in README.md was missing the "Pick teammates" entry (option 1 in the
  actual prompt).
- **/cancel** described as `/cancel 2`; takes `[task-id] [teammate]` now.

### Files changed
- `README.md` — onboarding table, REPL bullets, project structure
- `packages/cli/README.md` — Quick Start, Usage, Slash Commands table, Task Queue, Tabs section,
  Startup Lifecycle, Personas tiers, Architecture table, Testing table
- `docs/index.md` — 15 → 16 personas
- `docs/working-with-teammates.md` — /cancel/`interrupt` usage, Managing the Roster section,
  Working in Multiple Tabs section, /script, Quick Reference table additions
- `docs/cookbook.md` — point to `/add` for adding teammates, `/remove` for retiring,
  `/update` for template refresh, 15 → 16 personas
- `docs/adoption-guide.md` — 15 → 16 personas
- `ONBOARDING.md` — 15 → 16 personas with updated role list

### Key decisions
- Kept packages/cli/README.md edits — co-owned with Beacon but in-scope for Scribe's doc-consistency
  review role per SOUL.md.
- Did not touch template/README.md or template/PROTOCOL.md — they were already accurate for the
  current framework.
- Did not touch docs/teammates-memory.md — memory architecture is unchanged; prompt stack
  description still matches `system-prompt.ts`.

### Next task hints
- If the user wants a docs PR, package the commit and land it.
- Tabs and /interrupt could use a dedicated cookbook recipe down the line — skipped for this pass
  because working-with-teammates.md already covers them.

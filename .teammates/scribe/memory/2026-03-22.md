---
version: 0.6.1
type: daily
compressed: true
---

# 2026-03-22

## Code collision prevention — 5-layer defense model
- Designed: branch isolation, worktree isolation (S27), ownership routing, active claims (NEW), PR merge queue
- Key insight: semantic conflicts (incompatible refactors that merge cleanly) are the hard problem — ownership + claims solve this

## Collision prevention spec written
- Full spec at `.teammates/scribe/docs/specs/F-collision-prevention.md`
- Layer 4 claims: JSON in `.tmp/claims/`, advisory, auto-created on task assignment, 12h expiry
- Files changed: `.teammates/scribe/docs/specs/F-collision-prevention.md` (new), `.teammates/CROSS-TEAM.md`

## Claims must never be checked into git
- User decision: claims are NEVER committed. Phase 1 = single-machine only. Cross-machine requires Campfire server (Phase 2).
- Files changed: `.teammates/scribe/docs/specs/F-collision-prevention.md`

## Explained git worktrees to user
- Worktrees share git object database but have independent working dirs; one branch per worktree
- No files changed.

## Worktree strategy — per task with shared home base
- Recommended per-task worktrees; `.teammates/` operations stay in main worktree via absolute path
- Branch naming: `teammates/<agent>/<task-slug>`
- Claims moved to `.git/teammates/claims/` (shared `.git` dir visible to all worktrees)
- No files changed (design discussion).

## Baked worktree model into spec + handoff to Beacon
- Rewrote Layer 2: per-agent granularity, full lifecycle, smart detection (only create for files outside `.teammates/`)
- Handed off basic worktree lifecycle to @beacon
- Files changed: `.teammates/scribe/docs/specs/F-collision-prevention.md`

## SOUL.md "Prime Directive" update
- User directed @everyone: "do what you're told" as #1 rule → dedicated Prime Directive section above Continuity
- Updated Scribe's SOUL.md + both TEMPLATE.md files
- Files changed: `.teammates/scribe/SOUL.md`, `.teammates/TEMPLATE.md`, `template/TEMPLATE.md`

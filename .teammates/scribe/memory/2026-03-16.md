---
type: daily
compressed: true
---

# 2026-03-16

## Updated README with supported agents and startup instructions
- Replaced "Tool Agnostic" with "Supported Coding Agents" — per-agent startup instructions for Claude Code, Codex, Copilot
- Files changed: `README.md`

## Added docs site link to README
- Added `https://stevenic.github.io/teammates` link above "The Problem" section
- Files changed: `README.md`

## Removed migration guide
- Deleted `docs/migration-guide.md` per user — nobody has v2 yet, premature artifact
- Removed all references from 6 files
- Files changed: `docs/migration-guide.md` (deleted), `README.md`, `ONBOARDING.md`, `.teammates/CROSS-TEAM.md`, `docs/cookbook.md`, `docs/_layouts/default.html`, `docs/index.md`

## Updated CLI README slash commands table
- Rewrote table to match actual registered commands in `cli.ts` — removed 3 stale, added 4 missing
- Files changed: `packages/cli/README.md`

## Self-retrospective
- Evidence-based retro: boundary discipline working well; premature shipping, stale paths, ownership gaps not working
- 3 proposals submitted for user approval. No files changed — awaiting approval.

## Designed lightweight teammate import spec
- Import copies only SOUL.md + WISDOM.md per teammate, each gets adaptation task. Handed off to @beacon.
- No files changed.

## Implemented folder naming convention for .teammates/
- Convention: no prefix = teammate, `_` prefix = shared (checked in), `.` prefix = local (gitignored)
- Renamed `standups/` → `_standups/`, `tasks/` → `_tasks/`
- Files changed: `.teammates/_standups/` (renamed), `.teammates/_tasks/` (renamed), `.teammates/.gitignore`, `template/.gitignore`, `.teammates/PROTOCOL.md`, `template/PROTOCOL.md`, `.teammates/README.md`, `template/README.md`, `.teammates/CROSS-TEAM.md`, `.teammates/scribe/docs/STANDUP-FORMAT.md`, `ONBOARDING.md`
- Handed off CLI detection logic update to @beacon

## Explained session file purpose and lifecycle
- Traced through adapter code: session file = per-teammate scratch pad, created on first task, injected into prompt, destroyed on CLI exit
- No files changed.

## Diagnosed session file missing bug after import
- Identified 4 possible causes for missing `.tmp/sessions/<name>.md` after import; handed off to @beacon
- No files changed.

## Weekend work summary
- 22 items, 16 features/docs shipped, 2 bugs fixed. No files changed.

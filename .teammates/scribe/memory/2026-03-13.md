# 2026-03-13

## Created cli/README.md
- Full CLI documentation: quick start, usage, slash commands, autocomplete, task queue, handoffs, conversation history, adapters (built-in + custom), architecture overview, output protocol
- Updated main README.md CLI section to match (correct binary name, feature bullets)
- Files changed: `cli/README.md` (new), `README.md` (updated CLI section)

## Added private docs guidance to framework
- Teammates didn't know they could create additional private docs under their folder or share pointers via CROSS-TEAM.md
- Added Continuity bullet to: `template/TEMPLATE.md`, `.teammates/beacon/SOUL.md`, `.teammates/scribe/SOUL.md`
- Expanded Sharing section in `.teammates/PROTOCOL.md` with Private docs entry
- Updated `.teammates/CROSS-TEAM.md` header to mention its shared-index role
- Files changed: `template/TEMPLATE.md`, `.teammates/PROTOCOL.md`, `.teammates/CROSS-TEAM.md`, `.teammates/beacon/SOUL.md`, `.teammates/scribe/SOUL.md`

## Created shared Architecture doc
- Created `.teammates/scribe/docs/ARCHITECTURE.md` — full project architecture covering packages, data flow diagrams, key patterns, tech stack, build instructions
- Added pointer in `.teammates/CROSS-TEAM.md` under new "Shared Docs" section so Beacon can find it
- Covers: CLI orchestrator, Recall search, framework templates, task execution flow, memory lifecycle, handoff chains, dependency direction
- Files changed: `.teammates/scribe/docs/ARCHITECTURE.md` (new), `.teammates/CROSS-TEAM.md` (updated)

## CLI onboarding flow — boundary violation corrected
- Scribe incorrectly implemented the onboarding flow directly in `cli/src/cli.ts` and `cli/src/onboard.ts`
- User corrected: this should have been a handoff to @beacon
- Saved feedback memory: `memory/feedback_handoff_cli.md`
- **Action needed:** Hand off CLI implementation to Beacon with the behavioral spec

## Added boundary enforcement to framework docs
- Updated `PROTOCOL.md` (both `.teammates/` and `template/`) — added "Boundary Rule" subsection and rule #4 to Handoff Conventions
- Updated `template/TEMPLATE.md` — added guidance above the Boundaries list: hand off, don't implement
- Updated `ONBOARDING.md` — added tip about strict boundaries
- This ensures all future teammates (and new projects) get clear guidance: never modify files outside your ownership
- Files changed: `.teammates/PROTOCOL.md`, `template/PROTOCOL.md`, `template/TEMPLATE.md`, `ONBOARDING.md`

## Designed /install command, handed off to Beacon
- User asked for `/install <service>` command to install services like recall from within the CLI
- Designed the command spec: validates service name, runs npm install + build in service dir, verifies binary
- Updated `cli/README.md` with the new command in the slash commands table
- Handed implementation off to @beacon
- Files changed: `cli/README.md`

## Added ownership scopes and self-owned folder reinforcement
- User asked to reinforce that teammates own everything under `.teammates/<name>/` and to write ownership scopes to CROSS-TEAM.md
- Added **Ownership Scopes** section to `CROSS-TEAM.md` with scope table (teammate → self-owned folder → codebase ownership)
- Added **Self-owned folder exception** to Boundary Rule in both `.teammates/PROTOCOL.md` and `template/PROTOCOL.md`
- Updated `template/TEMPLATE.md` Boundaries section to lead with unconditional self-ownership
- Files changed: `.teammates/CROSS-TEAM.md`, `.teammates/PROTOCOL.md`, `template/PROTOCOL.md`, `template/TEMPLATE.md`

## Ensured CROSS-TEAM.md gets populated during onboarding
- User flagged that new projects should have CROSS-TEAM.md populated up front, not left empty
- Updated `template/CROSS-TEAM.md` — now includes Ownership Scopes section with placeholder table, Shared Docs section, and shared-index description
- Updated `ONBOARDING.md` Step 3a — renamed to "Copy and populate framework files", added instruction to fill in the Ownership Scopes table immediately after copying
- Added verification check in Step 4: "CROSS-TEAM.md Ownership Scopes table has one row per teammate with correct paths"
- Files changed: `template/CROSS-TEAM.md`, `ONBOARDING.md`

## Documented teammates-recall in PROTOCOL.md
- Added "## Available Tools" section to `.teammates/PROTOCOL.md` with teammates-recall command table
- Covers: search (all/scoped), sync, status commands + note about fully local operation
- Placed between "Cross-Cutting Concerns" and "Memory" sections
- Did NOT update `template/PROTOCOL.md` — teammates-recall is project-specific, not framework-generic
- Files changed: `.teammates/PROTOCOL.md`

## Fixed index path in docs: `.teammates/<name>/.index/`
- User corrected: indexes should be at `.teammates/<name>/.index/` not `.teammates/.index/<name>/`
- Updated 2 references in `.teammates/scribe/docs/ARCHITECTURE.md`
- Handed off to @beacon: update `recall/README.md`, `beacon/SOUL.md`, `beacon/MEMORIES.md`, and recall source code
- Files changed: `.teammates/scribe/docs/ARCHITECTURE.md`

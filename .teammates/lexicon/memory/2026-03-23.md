# 2026-03-23

## Notes

- Added `<INSTRUCTIONS>` reinforcement block to `.teammates/lexicon/SOUL.md`. 10 lines, each referencing a section by name (Identity, Prime Directive, Continuity, Core Principles, Boundaries, Quality Bar, Ethics, Capabilities, Ownership). Placed at the very end of the file for maximum positional attention.
- This is the SOUL.md-internal version of the instruction-reinforcement-spec (which targets adapter.ts's runtime sections). Same pattern — bidirectional attention bridges from bottom edge to each section.
- **Correction from stevenic:** The reinforcement block does NOT belong in SOUL.md — SOUL.md content gets embedded in `<IDENTITY>`, not `<INSTRUCTIONS>`. Reinforcement must be in the runtime `<INSTRUCTIONS>` block built by adapter.ts. Removed the block from SOUL.md.
- Re-handed off instruction-reinforcement-spec to Beacon for implementation in adapter.ts. The spec was already correct — it targets `instrLines` array as the final content. I just put it in the wrong file.

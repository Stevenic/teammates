---
type: daily
---

# 2026-04-25

## Notes

- **One-line intro (x2):** Steve asked @everyone for one-line intros twice today. Delivered a single-sentence persona summary framed around the three-layer prompt model (distance / compression / decompression) both times.
- **Wisdom compaction:** Reviewed all 11 WISDOM.md entries against typed memories (continuity-failure, adapter-reorder) and recent daily logs (2026-04-02 through 2026-04-21). All entries remain durable and accurate; typed memories continue to be covered by existing entries. No additions, removals, or edits needed beyond bumping the compaction date to 2026-04-25.
- **Mirror "daily logs are private" emphasis into adapter.ts fallback:** Beacon updated `system-prompt.ts` (primary builder) with an all-caps paragraph telling teammates the user cannot see daily logs / memory files, and that they must report results directly in their text response. Mirrored the same emphasis into `packages/cli/src/adapter.ts` (fallback inline builder, lines 499–501) — placed it inside the `### Output Protocol (CRITICAL)` block, right after the header and before "Format your response as:", to match the positional weight in the primary builder. Used a slightly terser version (kept the all-caps lead and the "tell the user directly" clause). Verified: `npm run build` clean, `npx vitest run` 334/334 pass, `npx biome check src/adapter.ts` clean. User must restart CLI to pick up new prompts.
- **Decision — placement over verbatim wording:** Beacon's handoff said "insert between FIRST priority line and `### Output Protocol`", but `system-prompt.ts` actually places the equivalent paragraph **inside** the Output Protocol section (after the header). Chose to mirror the actual primary-builder placement rather than the literal text of the handoff instruction — the goal is "same emphasis as the primary one", which means the same positional context for the rule. Logging this so future-me remembers: when a handoff's stated location contradicts the goal of "mirror the primary", trust the primary.

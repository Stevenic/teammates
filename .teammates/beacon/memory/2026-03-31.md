---
type: daily
---
# 2026-03-31

## Task: Strip version frontmatter from all teammate memory files
Ran a Node script across all `.teammates/*/memory/**/*.md` files to strip YAML frontmatter blocks (`---\nversion: ...\n---`). Covered both top-of-file frontmatter and embedded blocks (e.g., `version: 0.7.0` blocks inside weekly summaries). All 99 memory files across beacon, lexicon, pipeline, scribe, and stevenic were cleaned. Verified zero `---` blocks remain.
- Script stripped top-of-file `---...---` and embedded `---\nversion:...---` patterns
- All 5 teammates' memory directories processed: beacon (42), lexicon (10), pipeline (16), scribe (21), stevenic (5), plus weekly subdirs

## Task: Fix version stripping — remove only version: line, keep rest of frontmatter
Previous attempt incorrectly removed entire frontmatter blocks. Fixed to only strip the `version:` line while preserving all other frontmatter fields (type, week, period, compressed, name, description, etc.). Updated 99/99 files.
- Removed `version:` line emission from `compact.ts` (weekly summary, monthly summary, daily compression prompt x2)
- Removed `version:` references from `adapter.ts` memory update instructions (daily log and typed memory prompts)
- Cleaned up unused `PKG_VERSION` imports from both files
- Build verified clean
- Files changed: `packages/cli/src/compact.ts`, `packages/cli/src/adapter.ts`, all `.teammates/*/memory/**/*.md`

## Task: Update migration guide to strip version: from memory files
Updated `packages/cli/MIGRATIONS.md` to add a new `## 0.7.3` migration section that instructs the coding agent to strip `version:` lines from all memory file frontmatter. Also cleaned up stale `version:` references in older migration sections:
- **0.6.0 section**: Removed instructions to keep `version` field and to add `version: 0.6.0` to new frontmatter
- **0.7.0 section**: Removed the "Update version frontmatter" step and updated the section description
- **0.7.3 section (new)**: Added migration to strip `version:` lines from all memory files and embedded content, with before/after examples

### Files changed
- `packages/cli/MIGRATIONS.md`
- `.teammates/beacon/memory/2026-03-31.md`

## Task: Bump package versions to 0.7.4
Updated all three package versions and settings from 0.7.3 → 0.7.4.

### Files changed
- `packages/cli/package.json`
- `packages/recall/package.json`
- `packages/consolonia/package.json`
- `.teammates/settings.json`
- `package-lock.json`

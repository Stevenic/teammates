---
version: 0.6.0
type: daily
compressed: true
---
# 2026-03-22

## Task: Reorder prompt — instructions at the end
Moved Output Protocol, Session State, Memory Updates from mid-prompt to after Task section. Context/reference at top, task in middle, instructions at end (leverages recency effect). Files: adapter.ts

## Task: Add Prompt Engineer persona + scaffold @lexicon
Created persona template from prompt engineering expert. 7 Core Principles, capabilities, key techniques. Named "Lexicon", Tier 2. Scaffolded `.teammates/lexicon/` with SOUL.md + WISDOM.md. Key: co-owns adapter.ts + SOUL.md files, does NOT implement code. Files: personas/prompt-engineer.md, .teammates/lexicon/SOUL.md, .teammates/lexicon/WISDOM.md

## Task: Fix broken handoffs — agents not using fenced block format
Root cause: agents write handoffs in plain English, not ```handoff blocks. Handoff instruction was buried in Output Protocol with ambiguous nested backticks. Fix 1: separated handoff protocol into own `## Handoffs (IMPORTANT)` section with 4-space indentation example. Fix 2: added `findNaturalLanguageHandoffs()` fallback in cli-proxy.ts (only fires when zero fenced blocks found). Files: adapter.ts, cli-proxy.ts

## Task: Diagnosis — Why Lexicon doesn't remember
Root cause: system loads context correctly but agent doesn't use it. Lexicon has almost no memory (empty WISDOM.md, 2 recall entries). Contributing: no structural tags in prompt — daily log indistinguishable from other markdown.

---
type: daily
---

# 2026-04-10

## Task: Rewrite WISDOM.md with updated distilled entries
Rewrote `.teammates/beacon/WISDOM.md` with updated compaction date (2026-04-10). Incorporated learnings from 04-02 work that wasn't fully captured in the previous compaction.

### Key changes
- **Activity tracking section overhauled** -- replaced stale "PostToolUse hooks were removed" with accurate description: hooks were *restored* via `ensurePostToolUseHook()` no-op hook installer. Added multi-signal parsing entry (hooks, subagents, hook-free fallbacks).
- **Added `/add`, `/remove`, `/update` entry** -- documents the replacement of monolithic `/init` with three focused teammate management commands.
- **Added `isSolo` persistence entry** -- documents the settings.json flag that skips re-prompting for team onboarding.
- **Updated handoffs entry** -- added parallel vs sequential chain guidance.
- **Updated ESM entry** -- strengthened to note that bare `require()` causes runtime errors and ESM compliance tests enforce this across all packages.
- **Updated shiftAllContainers entry** -- clarified that activity index shifting must be wired into the ThreadManager callback so ALL feed insertions update activity indices (fix for multi-activity cleanup bug).
- **Tightened prose** throughout, removed redundant phrasing.

### Files changed
- `.teammates/beacon/WISDOM.md` -- full rewrite with 2026-04-10 compaction date
- `.teammates/beacon/memory/2026-04-10.md` -- created daily log

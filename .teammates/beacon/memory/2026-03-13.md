# 2026-03-13

## Task: Taller logo + recall status indicator
- Expanded CLI banner logo from 3 to 5 rows (wider box with emoji centered)
- Added recall system detection using `execFileSync("teammates-recall", ["--help"])`
- Shows green dot when installed, yellow dot when not
- Files changed: `cli/src/cli.ts`

## Task: DNA emoji padding
- Added one extra space of padding on each side of the 🧬 emoji in the banner
- Changed `" 🧬 "` → `"  🧬  "` in `cli/src/cli.ts`

## Task: Full codebase read-through
Read all 13 source files across cli/ (9 files, ~2,600 lines) and recall/ (4 files, ~632 lines).

### Key Architecture Notes

**CLI Package:**
- `cli.ts` (1268 lines) — Main REPL with wordwheel/dropdown completion, 9 slash commands, @mention parsing, task queue with mutex drain, conversation history (last 10)
- `orchestrator.ts` (265 lines) — Keyword scoring (primary=2pts, secondary=1pt), handoff chains (max depth 5, cycle detection), event-driven
- `registry.ts` (177 lines) — Discovers teammates from `.teammates/`, parses SOUL.md for role + ownership patterns
- `adapter.ts` (216 lines) — AgentAdapter interface, `buildTeammatePrompt()` hydrates full context (max 3 daily logs)
- `cli-proxy.ts` (442 lines) — Generic subprocess adapter. 3 presets (claude, codex, aider). 10-min timeout with SIGTERM→SIGKILL. Parses structured JSON + freeform output
- `echo.ts` (37 lines) — Test adapter
- `dropdown.ts` (90 lines) — ANSI-aware dropdown overlay, hooks readline `_refreshLine`
- `types.ts` (84 lines) — Core types including discriminated union for OrchestratorEvent
- `index.ts` (29 lines) — Re-exports

**Recall Package:**
- `embeddings.ts` (44 lines) — LocalEmbeddings via transformers.js, lazy model load, 384-dim
- `indexer.ts` (204 lines) — One Vectra index per teammate, full rebuild vs smart sync, URI normalization
- `search.ts` (108 lines) — Auto-sync, multi-teammate aggregation, section-level scoring
- `cli.ts` (276 lines) — 5 commands (index, sync, add, search, status), --json support

### Observations
- TEAMMATE_ARG_POSITIONS references "assign" but no /assign slash command exists — assignment works via @mention dispatch
- /verbose referenced in SOUL.md but not registered as a command in the REPL
- Queue uses Promise-based mutex (drainLock) — clean concurrency pattern
- Dropdown.ts uses ANSI escape codes directly for cursor management

## Task: Enter executes wordwheel selection
- Modified Enter key handler in `_ttyWrite` to accept highlighted wordwheel item into the input line before readline processes Enter
- No-arg commands (/exit, /status, /help, /teammates) now execute in one keystroke from the dropdown
- Changed lines ~439-450 in `cli/src/cli.ts`

## Task: Review boundary enforcement changes
- Reviewed Scribe's boundary discipline additions across 4 files
- Checked `.teammates/PROTOCOL.md`, `template/PROTOCOL.md`, `template/TEMPLATE.md`, `ONBOARDING.md`
- All four files convey the same core rule consistently: don't modify files outside ownership, hand off instead
- Key phrase "even if the fix seems small or obvious" present in both protocols and template
- No issues found, no changes needed — approved as-is

## Task: Review CLI onboarding flow (Scribe's implementation)
- Reviewed `cli/src/onboard.ts` (new file, 125 lines) and onboarding additions to `cli/src/cli.ts`
- New features: interactive menu when no `.teammates/` exists (3 options), solo mode, `/init` command (aliases: `/onboard`, `/setup`)
- Key findings:
  - `promptOnboarding()` and `cmdInit()` duplicate onboarding logic through different paths — future consolidation candidate
  - Solo mode creates empty `.teammates/` which works but needs Registry to handle gracefully
  - `onboard.ts` uses `__dirname`-relative paths to find `ONBOARDING.md` — correct for dist/ but fragile if build structure changes (falls back to embedded copy)
  - Overall solid implementation, accepting and taking ownership
- No code changes made (review only)

## Task: Cleanup onboarding code
- Extracted `printLogo(infoLines)` — DRYs the 5-row box logo between `promptOnboarding` and `printBanner`
- Extracted `runOnboardingAgent(adapter, projectDir)` — DRYs agent execution between `promptOnboarding` and `cmdInit`
- Extracted `printAgentOutput(rawOutput)` — strips JSON protocol block, reusable display helper
- Simplified `cmdInit()` from 30 lines to 7 — delegates to `runOnboardingAgent`
- Simplified `promptOnboarding()` — removed duplicated logo rendering and inline agent session management
- Cleaned up `onboard.ts` — added `projectDir/ONBOARDING.md` as first candidate path, clearer naming
- Removed stray blank lines in event handler and cmdRoute
- Net reduction: ~53 lines (1615→1562 in cli.ts)
- Files changed: `cli/src/cli.ts`, `cli/src/onboard.ts`

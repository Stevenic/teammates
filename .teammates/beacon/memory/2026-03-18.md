---
compressed: true
---
# 2026-03-18

## Task: Hand off README/docs update to Scribe
Gathered CLI onboarding technical details and handed off to Scribe. Current README still references old "clone repo + point agent at ONBOARDING.md" flow.

## Task: Diagnose empty teammate responses
Root cause: 5 layers of prompt size explosion. (1) storeResult stores full rawOutput, (2) no token budget on conversation context, (3) context prepended before task, (4) large base prompt overhead, (5) @everyone amplifies everything. Proposed 3 tiers of fixes.

## Task: Implement context window budget management
Changed `storeResult()` to use `result.summary` instead of `rawOutput`. Added 16k char budget to `buildConversationContext()`. Refactored `buildTeammatePrompt()` to priority-based system: weekly (10) → older daily (25) → recent daily (35) → recall (40) → current daily (50) → identity/wisdom/protocol (100, never trimmed). 64k token wrapper budget, task excluded. Files: cli.ts, adapter.ts, adapter.test.ts

## Task: Refine priority tiers + remove session content
Three-tier daily logs (today=50, days 2-3=35, days 4-7=25). Removed session content entirely (duplicated conversation history). Files: adapter.ts, adapter.test.ts, cli-proxy.ts, copilot.ts

## Task: Per-section token budgets
Replaced priority-drop with per-section truncation: recall 10k, previous daily 8k, older daily 8k, weekly 6k. Current daily → priority 100 (never trimmed). Truncation marker instead of dropping. Files: adapter.ts, adapter.test.ts

## Task: Bottom-up budget allocation
Replaced per-section truncation with linear bottom-up allocator. Tiers: older daily → previous daily → recall → weekly. Whole entries only. Removed PromptSection type. Simplified prompt assembly. Files: adapter.ts, adapter.test.ts

## Task: Split budget — 24k daily logs + 8k minimum recall
Daily logs (days 2-7): 24k token pool. Recall: max(8k, 8k + unused daily). 4k overflow grace for last recall entry. Weekly summaries removed from prompt (recall indexes them). Files: adapter.ts, adapter.test.ts

## Task: Conversation history budget with auto-summarization
24k token budget for recent conversation, running summary of older history. `maybeQueueSummarization()` fires after every storeResult. `"summarize"` QueueEntry type — invisible to user, skips context/history/debug. Summarization prompt builds incrementally. Files: types.ts, cli.ts

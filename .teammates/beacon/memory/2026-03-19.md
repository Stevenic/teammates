---
version: 0.7.0
type: daily
compressed: true
---
# 2026-03-19

## Task: Remove large response temp file dump
Removed the `sizeKB > 5` branch that wrote responses to OS temp files. All responses now flow through `feedMarkdown()`. Files: cli.ts

## Task: Review Multi-Human Collaboration Spec
Reviewed Scribe's spec. Phase 1 (file-based) viable — extends existing handoff parsing. Phase 2 (server) feasible but needs scoping. Key concern: "AI teammates execute via the server" has major compute/cost implications — recommend keeping AI execution client-side. Handoff threads deferred to Phase 2.5.

## Task: Implement user avatar system (Campfire Phase 1)
Users represented as avatar teammates. Added `TeammateType` ("human"|"ai"), `PresenceState`, `type` field on TeammateConfig. Interview collects alias. `createUserAvatar()` creates folder with SOUL.md + WISDOM.md. `selfName` = userAlias ?? adapterName. /status shows presence indicators. @everyone excludes avatar + adapter. Files: types.ts, orchestrator.ts, registry.ts, cli.ts, template/USER.md, tests

## Task: Hide adapter identity — show user alias everywhere
Banner shows user alias instead of "claude". Chat feed label uses selfName. Conversation history uses selfName. All display surfaces map adapterName → selfName. Removed claude example from usage. Files: cli.ts, banner.ts, startup.ts, cli-args.ts

## Task: Remove coding agent as addressable teammate
Adapter not registered when user has alias. All routing fallback targets selfName. Internal tasks target selfName. Banner/roster exclude both adapterName and userAlias. Files: cli.ts, orchestrator.ts

## Task: Banner presence indicators + user avatar in roster
Extended BannerInfo.teammates with presence + isUser. Presence-colored dots (green/yellow/red). User first with (you) label. Files: banner.ts, cli.ts

## Task: Segmented footer bar (left/right)
Added `_footerRight` to ChatView. `setFooterRight()` method. Left: product + version. Right: `? /help` default, temporarily ESC/Ctrl+C hints. Files: chat-view.ts, cli.ts

## Task: Fix /configure github — feed-based instructions
Rewrote `configureGitHub()` to show commands in feed instead of spawning with `stdio: "inherit"`. Removed spawn import. Files: cli.ts

## Task: Banner cleanup — remove user from roster, add agent name to footer
Removed user avatar from banner roster (already shown in header). Added adapter name to footer left segment. Removed `isUser` from BannerInfo. Files: cli.ts, banner.ts

## Task: Fix /configure github — mouse escape sequences
Root cause: `askInput()` creates raw readline that fights consolonia mouse tracking. Added `askInline()` using ChatView's own input system with `_pendingAsk` resolver. Files: cli.ts

## Task: Fix drag-to-select releasing to wrong region
Root cause: `_feedY`/`_feedH` only updated inside scrollbar-visible branch. Moved updates before the conditional. Added `_selEnd` update in release handler. Files: chat-view.ts

## Task: /configure UX improvements (3 fixes)
(1) Banner live update via `updateServices()` on AnimatedBanner. (2) `feedCommand()` helper with [copy] action for bash commands. (3) `askInline()` shows prompt in feed before footer. Files: banner.ts, cli.ts

## Task: Three UX fixes — emoji spacing, image paste, paste @mention routing
(1) Extra space after ✔/✖. (2) Trailing space on image placeholder. (3) Pre-resolve @mentions from raw input before paste expansion via `preMentions` parameter. Files: cli.ts

## Task: Move user onboarding to pre-TUI with GitHub option
`runUserSetup()` offers: GitHub / Manual / Skip. `setupGitHubProfile()` imports from `gh api user`. `setupManualProfile()` via console prompts. All pre-TUI via console.log + askInput. Removed Interview widget. Files: cli.ts

## Task: Global ✔/✖/⚠ emoji spacing sweep
Fixed all 15 emoji instances in cli.ts for consistent double-space. Files: cli.ts

## Task: Fix GitHub onboarding crashes
Run `gh auth login` directly with spawnSync (pre-TUI safe). Removed `registerUserAvatar()` from `createUserAvatar()` (orchestrator not yet initialized). Files: cli.ts

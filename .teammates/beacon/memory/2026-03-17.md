---
version: 0.6.0
type: daily
compressed: true
---
# 2026-03-17

## Task: Add process diagnostics for empty agent responses
Added `SpawnResult` type with separate stdout/stderr/exitCode/signal. Claude preset now uses `--debug-file` for internal debug logs. Added `TaskResult.diagnostics`. `writeDebugEntry` logs process metadata. Empty response UI shows exit code/signal warnings. Files: cli-proxy.ts, types.ts, cli.ts

## Task: Ctrl+click file paths + default routing to active teammate
Extended ChatView ctrl+click for absolute file paths (Windows + Unix). Added `"file"` event. Default routing now uses `lastResult.teammate` → `orchestrator.route()` → `adapterName`. Files: chat-view.ts, cli.ts

## Task: Gate debug file behind --debug flag
Added `--debug` CLI flag. Debug file only generated when flag present. Files: cli.ts, cli-proxy.ts

## Task: Research debug logging for Codex & Copilot
Only Claude supports `--debug-file`. Codex uses JSONL stdout, Copilot uses JSON-RPC. No changes needed.

## Task: Debug logging overhaul — .tmp/debug/ files
Always write structured debug logs to `.teammates/.tmp/debug/`. All adapters covered. 1-day cleanup on startup. `/debug` reads last log + queues analysis to coding agent. Added `"debug"` QueueEntry type. Files: cli.ts, cli-proxy.ts, SOUL.md

## Task: Add debug log file link to /debug output
Shows ctrl+clickable path to raw debug log before queuing analysis. Files: cli.ts

## Task: Bump all package versions to 0.3.0
cli, consolonia, recall all bumped to 0.3.0. Files: 3 package.json + lockfile

## Task: Self-retrospectives (2 sessions)
Found SOUL.md staleness: Key Interfaces missing packages/ prefix, Technologies "both packages", Ethics session file location. WISDOM.md line counts outdated.

## Task: Apply approved retro proposals 1-3
Fixed Key Interfaces paths, Technologies wording, Ethics session file location. Files: SOUL.md

## Task: Fix /compact @mention not stripping @ prefix
Added `.replace(/^@/, "")` to cmdCompact arg parsing. Files: cli.ts

## Task: Fix @everyone accent color in input box
Added "everyone" to both `validNames` sets in colorize callbacks. Files: cli.ts

## Task: Add wisdom distillation to /compact
Added `buildWisdomPrompt()` to compact.ts. After episodic compaction, queues agent task to rewrite WISDOM.md from typed memories + daily logs. Files: compact.ts, cli.ts

## Task: Bundle recall as dependency + automatic pre-task context
Added `@teammates/recall` as cli dependency. `queryRecallContext()` runs pre-task with `skipSync: true`. `syncRecallIndex()` replaces subprocess calls. Removed watch process. Recall section between WISDOM and daily logs. Files: cli/package.json, adapter.ts, cli-proxy.ts, copilot.ts, cli.ts, index.ts

## Task: Inject session history into agent prompt
Added `sessionContent` option to `buildTeammatePrompt`. Both adapters read session file and pass content. Files: adapter.ts, cli-proxy.ts, copilot.ts

## Task: Remove /install command
Removed SERVICE_REGISTRY, cmdInstall, readSettings/writeSettings/isServiceInstalled, service completions, service injection. Banner always shows "● recall bundled". Files: cli.ts, SOUL.md

## Task: Update goals for Claude Code parity roadmap
Rewrote project_goals.md with CP1-CP10 parity features. N1 (decompose cli.ts) at #1 prerequisite. Files: project_goals.md

## Task: Update SOUL.md Continuity for bundled recall
Changed recall bullet to "automatically provided in your context via recall search." Files: SOUL.md

## Task: Update goals — two-tier model (Universal + Enhanced)
Added Enhanced features (EP1-EP6) + Adapter Infrastructure (AI1-AI2). MCP at P0 Enhanced. Files: project_goals.md

## Task: Vote on brainstormed feature ideas
Distributed 25 points across team brainstorm ideas. Weighted toward existing infrastructure leverage.

## Task: Update goals with team-unique features
Added TF1-TF6 features. TF2 (Decision & Memory Synthesis) identified as foundational piece. Files: project_goals.md

## Task: Stack-rank all goals
Single unified priority list of 27 goals. N1 at #1, CP2 at #2, N2 at #3. Optimal parallel execution identified. Files: project_goals.md

## Task: Suppress transformers.js dtype warning
Temporarily suppress `console.warn` during `pipeline()` call. Explicitly pass `dtype: "fp32"`. Files: embeddings.ts

## Task: Auto-scroll feed during drag-to-select
Interval-based auto-scroll (3 lines/80ms) when mouse exits feed area during selection. Timer cleanup in release + clearSelection. Files: chat-view.ts

## Task: Bump to 0.3.1
All 3 packages 0.3.0 → 0.3.1. Files: 3 package.json + lockfile

## Task: N1 decomposition — Steps 1 & 2 (cli-args.ts + banner.ts)
Extracted ~513 lines from cli.ts. New modules: cli-args.ts (152 lines: PKG_VERSION, parseCliArgs, findTeammatesDir, resolveAdapter, printUsage), banner.ts (359 lines: AnimatedBanner, BannerInfo). Moved tp to theme.ts, QueueEntry/SlashCommand to types.ts. Files: cli-args.ts, banner.ts, theme.ts, types.ts, cli.ts, index.ts

## Task: Spec review — S16, S17, S26, F1, F3
Reviewed all 5 specs. Key feedback: shell platform detection for hooks, `-p` arg parsing, MCP temp file cleanup, temporal engine query parsing. All implementation-ready.

## Task: Implement /configure + banner service status
Added dynamic service status in banner (recall + GitHub). `detectGitHub()` checks gh CLI + auth. `configureGitHub()` guides install + auth. `/configure` aliases: `config`. Files: banner.ts, cli.ts, index.ts, SOUL.md

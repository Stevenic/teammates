---
version: 0.7.0
type: daily
compressed: true
---
# 2026-03-21

## Task: Harden empty response handling — synthetic fallback + second retry
Added synthetic fallback in `displayTaskResult` — generates body from changedFiles + summary when cleaned output is empty. Added second retry with minimal "just say Done" prompt. Three layers: retry 1 (full summary, raw) → retry 2 (minimal, raw) → synthetic fallback. Files: cli.ts

## Task: Fix display-before-retry race condition
Root cause: `task_completed` event displayed result BEFORE defensive retry ran. Extracted `displayTaskResult()` method. Moved display call to `drainAgentQueue()` AFTER retry merge. Stripped `task_completed` handler to tracking/spinner only. Files: cli.ts

## Task: Fix selection highlight persisting after scroll
Added `clearSelection()` in `scrollFeed()`, scrollbar click-to-position, and scrollbar drag. Guard with `!this._selecting` for drag-to-select auto-scroll. Files: chat-view.ts

## Task: User display alias — show user as @adapter in presence/picker
User avatar displays as `@claude` (adapter name) in roster/picker/status. Messages still show real alias as sender. Memory updates go to user's folder. All 15 display mapping sites flipped to `=== selfName ? adapterName : x`. Files: cli.ts

## Task: Fix agent display role in roster/status
Replaced `registry.get(userAlias).role` with hardcoded "Coding agent that performs tasks on your behalf." at all 3 display sites. Files: cli.ts

## Task: Add platform/environment instruction to teammate prompts
Added OS detection via `platform()` with Windows/macOS/Linux mapping. `**Environment:**` line with slash convention guidance. Files: adapter.ts

## Task: Replace Context with Primary Timezone in user profiles
Auto-detect via `Intl.DateTimeFormat()`. Removed "Context" from interview. Added `**Primary Timezone:**` to USER.md and SOUL.md. `buildTeammatePrompt()` extracts timezone from USER.md via regex. Files: cli.ts, adapter.ts, template/USER.md

## Task: Update Steve's USER.md
Replaced `**Context:** nope` with `**Primary Timezone:** America/Los_Angeles`. Files: .teammates/USER.md

## Task: Role reframe discussion
Scribe → PM, Beacon → Software Engineer, Pipeline → DevOps. Discussion only.

## Task: Recall query architecture — Two-pass implementation
Pass 1: `extractKeywords()`, `buildQueryVariations()` (1-3 queries), `scanMemoryCatalog()`, `matchMemoryCatalog()`, `multiSearch()` with dedup. Pass 2: recall tool section in every teammate prompt for mid-task agent queries. Enhanced `queryRecallContext` in adapter.ts. 21 new tests. Files: query-expansion.ts, memory-index.ts, search.ts, recall/index.ts, adapter.ts, tests

## Task: Diagnosis — Scribe empty responses
Both debug logs show same pattern: agent reads files, edits memory/session, ends turn without text. Contributing: instruction ordering, buried output protocol, large prompt overhead. Recommended: prompt restructuring, two-phase execution, defensive retry.

## Task: Fix empty teammate responses — two-phase + defensive retry
Moved Output Protocol before Session/Memory. Changed to response-first ordering. Added defensive retry with raw mode in drainAgentQueue. Files: adapter.ts, cli.ts

## Task: Make defensive retry silent
Added `silentAgents` set to suppress all events during retry. Files: cli.ts

## Task: Discussion — "Mostly Humans" onboarding composition
Lightweight composition: Scribe (PM) + Sentinel (QA) only. Approved, implementation deferred.

## Task: Wire persona templates into CLI onboarding
New personas.ts module. Restructured startup menu with "Pick teammates" as option 1. Pre-TUI and in-TUI (`/init pick`) persona pickers. 15 persona templates. Files: personas.ts, cli.ts, index.ts, package.json

## Task: Fix defensive retry — raw mode
Root cause: retry used full prompt wrapping, causing same failure. Added `raw?: boolean` to TaskAssignment — skips buildTeammatePrompt entirely. Files: types.ts, adapter.ts, orchestrator.ts, all adapters, cli.ts

## Task: Diagnosis — empty response recurrence
Root cause: CLI running old code. Node.js caches modules at startup. Restart required after rebuilds.

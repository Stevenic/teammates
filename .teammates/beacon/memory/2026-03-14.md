---
compressed: true
---
# 2026-03-14

## Task: Three-tier memory migration (MEMORIES.md → WISDOM.md)
Handoff from Scribe. Renamed `TeammateConfig.memories` → `wisdom`, updated registry to filter daily logs by YYYY-MM-DD pattern, updated adapter prompt for typed memories + read-only WISDOM.md. Distilled old MEMORIES.md into 5 WISDOM entries. Files: types.ts, registry.ts, adapter.ts, onboard.ts, cli.ts, cli-proxy.ts, tests, templates, recall/indexer.ts, SOUL.md, WISDOM.md

## Task: Episodic memory compaction system
Full implementation per Scribe's spec. CLI: added WeeklyLog/MonthlyLog types, weekly log loading, weekly summaries in prompts, `/compact` command, recall watch mode. Compact.ts (new): `compactDailies()` groups by ISO week, `compactWeeklies()` archives >52 weeks. Recall: skip raw dailies from indexing, multi-pass retrieval (recency + semantic), typed memory 1.2x boost, `watch` command with fs.watch. Key decisions: raw dailies not indexed (in prompt), recency pass reads weekly files from disk, current week never compacted. Files: types.ts, registry.ts, adapter.ts, compact.ts (new), cli.ts, indexer.ts, search.ts, recall/cli.ts, tests

## Task: Increase daily log injection from 3 to 7
Changed adapter.ts `slice(0, 3)` → `slice(0, 7)` to cover full week until compaction. Files: adapter.ts, adapter.test.ts

## Task: Make /compact use the task queue
Added `QueueEntry` discriminated union (`agent | compact`). `cmdCompact` pushes compact entries, `drainQueue` routes by type. New `runCompact()` with spinner + recall sync. Files: cli.ts

## Task: Simplify CLI input box
4 UX changes: prompt `>` instead of `teammates>`, @mention/@command syntax coloring, tab auto-selects first item, input wraps across rows. Files: prompt-input.ts, interactive-readline.ts, cli.ts

# 2026-03-14

## Task: Three-tier memory migration (MEMORIES.md → WISDOM.md)

Handoff from Scribe. Migrated CLI, recall, templates, and Beacon's own files from the old two-layer memory system (MEMORIES.md + daily logs) to the new three-tier system (Daily Logs → Typed Memories → WISDOM.md).

### Key decisions
- Renamed `TeammateConfig.memories` → `TeammateConfig.wisdom` throughout (field name change, not just file references)
- Registry's `loadDailyLogs()` now filters to YYYY-MM-DD pattern only, so typed memory files in `memory/` aren't loaded as daily logs
- Adapter prompt tells teammates to write typed memories (not edit MEMORIES.md) and marks WISDOM.md as read-only (compaction only)
- Distilled old MEMORIES.md into WISDOM.md with 5 wisdom entries

### Files changed
- `cli/src/types.ts`, `cli/src/registry.ts`, `cli/src/adapter.ts`, `cli/src/onboard.ts`, `cli/src/cli.ts`
- `cli/src/registry.test.ts`, `cli/src/adapter.test.ts`, `cli/src/orchestrator.test.ts`
- `cli/src/adapters/cli-proxy.ts`, `cli/src/adapters/echo.test.ts`
- `cli/template/TEMPLATE.md`, `cli/template/PROTOCOL.md`, `cli/template/README.md`, `cli/template/CROSS-TEAM.md`
- `cli/template/example/SOUL.md`, `cli/template/example/WISDOM.md` (new), `cli/template/example/MEMORIES.md` (deleted)
- `recall/src/indexer.ts`, `recall/README.md`, `recall/package.json`
- `.teammates/beacon/SOUL.md`, `.teammates/beacon/WISDOM.md` (new), `.teammates/beacon/MEMORIES.md` (deleted)

### Build & test
- Both packages compile cleanly with strict mode
- All 55 CLI tests pass

---
version: 0.7.0
type: daily
compressed: true
---
# 2026-03-16

## Task: Services as objects in settings.json
Changed services from string array to object array in settings.json. Updated read/write/check helpers. Files: cli.ts, .teammates/settings.json

## Task: Move packages to packages/ directory
Moved consolonia, recall, cli into `packages/` via `git mv`. Updated workspaces, biome, CI/CD, dependency paths. 763 tests pass. Files: package.json, biome.json, workflows, cli/package.json

## Task: Add README to each package
Added monorepo back-links to CLI and recall READMEs. Created consolonia README with features/architecture/testing. Files: 3 README.md files

## Task: Multi-mention dispatch
Changed `queueTask()` to dispatch to ALL @mentioned teammates (not just first). Single regex scan, full unmodified input per teammate. Files: cli.ts

## Task: Ctrl+click to open web links in browser
Added URL detection to ChatView ctrl+click handler with `_screenToFeedRow` map, `_findUrlAtOffset()`. CLI opens links via platform command. Multi-URL lines use position-based hit testing. Files: chat-view.ts, cli.ts

## Task: Command placeholder hints
Dim placeholder hints after typed commands (e.g., `/log [teammate]`). Added `hint` callback to PromptInput. `getCommandHint()` parses input and returns remaining placeholders. Files: prompt-input.ts, cli.ts

## Task: Banner quick reference updates
Replaced /debug + /log with /compact + /retro. Added first-run mode (2×2 grid with /init, /install, /help, /exit). Files: cli.ts

## Task: Self-retrospective
Found 6 SOUL.md proposals — path staleness from packages/ move, outdated commands/interfaces.

## Task: Apply retro Proposals 1, 2, 5, 3, 4, 6
Fixed all SOUL.md staleness: ownership paths → `packages/` prefix, file patterns with consolonia, 4 missing Key Interfaces (compact, onboard, cli-utils, copilot), updated REPL commands list, "all three packages" in Quality Bar, added Biome + Vitest to Technologies. Files: SOUL.md

## Task: Fix command placeholder hints not showing in ChatView
Added `hint` callback + `hintStyle` to TextInput widget, pass-through in ChatView. Files: text-input.ts, chat-view.ts, cli.ts

## Task: Remove /log command
Removed command registration, TEAMMATE_ARG_POSITIONS entry, `cmdLog()`, `printTeammateLog()`. Files: cli.ts, SOUL.md

## Task: Add /user command
`/user` shows USER.md; `/user [change]` queues agent task to update it. Files: cli.ts, SOUL.md

## Task: Word-wrap user messages in feed
User messages now word-wrap on spaces via `wrapLine()`. Files: cli.ts

## Task: Add "everyone" option to teammate-arg commands
/compact, /retro, /debug now support "everyone" via wordwheel dropdown. Files: cli.ts

## Task: Read version from package.json
Added `PKG_VERSION` constant reading from `../package.json`. Replaced 6 hardcoded version strings. Files: cli.ts

## Task: Standardize placeholder format
Unified command usage strings to `[square]` brackets. Files: cli.ts, SOUL.md

## Task: Add /btw command
Quick side questions that skip conversation context and history. Added `"btw"` QueueEntry type. Files: cli.ts

## Task: Import teammates from another project
Added `importTeammates()` and `buildAdaptationPrompt()` to onboard.ts. Startup menu: New/Import/Solo/Exit. `/init [path]` triggers import + adaptation. Files: onboard.ts, cli.ts

## Task: Lightweight import — only SOUL.md + WISDOM.md
Refactored to copy only SOUL.md + WISDOM.md per teammate. Per-teammate adaptation with full context in prompt. Files: onboard.ts, cli.ts

## Task: Skip project-specific framework files during import
Only copy USER.md from top-level. Framework files created fresh from template. Files: onboard.ts

## Task: USER.md interview on startup
Interactive interview when USER.md missing/has placeholders. 5 questions, all optional. Files: cli.ts

## Task: Apply folder naming convention
Registry skips `_` and `.` prefixed dirs. Replaced hardcoded `NON_TEAMMATE_ENTRIES` with prefix-based `isNonTeammateEntry()`. Files: registry.ts, onboard.ts, registry.test.ts

## Task: Fix cursor position after /command wordwheel selection
Removed placeholder params from completion string — commands complete to `/{name} `. Files: cli.ts

## Task: Fix CI lint errors
Fixed 6 biome errors: formatting auto-fixes + removed unused private members in chat-view.ts. Re-added `dispatching` property. Files: chat-view.ts, cli.ts, onboard.ts, registry.ts, prompt-input.ts

## Task: Fix startup banner drift
Synced startup animation columns to match printBanner layout. Files: cli.ts

## Task: Fix session files missing after import
Set `cwd: dirname(this.teammatesDir)` on every teammate config in registry.loadTeammate(). Updated .gitignore from `.index/` to `.*/`. Files: registry.ts, onboard.ts

## Task: Fix markdown table column shrinking + Windows ctrl+click
Column shrinking: lock small columns at natural width. Windows URL: `start "" "url"` (empty title). Files: markdown.ts, cli.ts

## Task: Debug entries in session files + /debug reads session
Added `getSessionFile?()` to adapter interface. `writeDebugEntry()` appends diagnostics to session file. `/debug` reads last session debug entry. Files: adapter.ts, cli-proxy.ts, copilot.ts, cli.ts

## Task: Revamp import adaptation flow
Single comprehensive agent session (was per-teammate). 7 phases: scan → evaluate → adapt → delete → create → update framework → verify. "Previous Projects" section captures compressed history. Files: onboard.ts, cli.ts

## Task: Rename services.json → settings.json
Migrated to `{ version: 1, services: [...] }` format. Added readSettings/writeSettings/isServiceInstalled helpers. Files: cli.ts, onboard.ts

## Task: Fix example/ loaded as teammate
Added `NON_TEAMMATE_NAMES` set with "example" to registry. Added warnings to adaptation/onboarding prompts. Files: registry.ts, onboard.ts, registry.test.ts

## Task: Fix concurrent session startup race
Moved `sessionsDir` assignment after `await mkdir()` completes. Files: cli-proxy.ts, copilot.ts, cli.ts

## Task: Fix adaptation not rewriting imported teammates
Removed approval gates from adaptation prompt (agent runs in non-interactive mode). Made adapting existing teammates first priority. Files: onboard.ts

## Task: Fix /init re-adaptation
`importTeammates()` now returns `{ teammates, skipped, files }`. cmdInit combines both for re-adaptation. Files: onboard.ts, cli.ts

## Task: Fix session directory ENOENT on /install
mkdir runs on every startSession call. cleanOldTempFiles skips sessions dir. Files: cli-proxy.ts, copilot.ts, cli.ts

## Task: Feedback — clean dist before rebuild
Always `rm -rf dist && npm run build` after code changes. Saved as feedback memory.

---
compressed: true
---
# 2026-03-20

## Task: Include USER.md in task context window
Added `userProfile` option to `buildTeammatePrompt()`. Adapters read USER.md and pass content. Placed just before task section, not budget-trimmed. Files: adapter.ts, cli-proxy.ts, copilot.ts

## Task: Reorder startup onboarding — user first, then team
Restructured startup: detect missing .teammates/ → create dir → check USER.md → runUserSetup() → if new project: promptTeamOnboarding(). Renamed `promptOnboarding()` → `promptTeamOnboarding()`. Files: cli.ts

## Task: "Hello AI Gym!" feedback memory
Temporary demo rule. Later removed when demo ended. Files: feedback_demo_subject.md (created then deleted)

## Task: Weighed in on Hands project
Identified 5 Beacon deliverables for P2 computer use: MCP server lifecycle (~200 LOC), adapter preset capabilities (~100 LOC), MCP config injection (~80 LOC), /configure hands (~60 LOC), visual context indexing (~150 LOC stretch). EP1 (MCP Integration) is prerequisite.

## Task: Fix GitHub onboarding — missing profile fields
Added experience, preferences, context prompts to `setupGitHubProfile()`. Files: cli.ts

## Task: Hand off onboarding docs update to Scribe
Handed off with full context on all onboarding changes from 03-19 and 03-20.

## Task: Self-retrospective
Found 3 SOUL.md proposals: missing Key Interfaces for banner.ts/cli-args.ts/theme.ts, missing Consolonia capabilities, stale CLI Commands description.

## Task: Apply retro proposals (2 rounds)
Round 1: Added 3 Key Interfaces entries (banner, cli-args, theme). Round 2: Added Consolonia Capabilities subsection (9 capabilities), updated CLI Commands from agent-first to avatar-first. Files: SOUL.md

## Task: Recall query architecture — Two-pass implementation
Pass 1 (no LLM): `buildQueryVariations()` generates 1-3 queries, `matchMemoryCatalog()` does frontmatter text matching, `multiSearch()` fuses with dedup. Pass 2 (mid-task): recall tool section in every teammate prompt for agent-driven iterative queries. Key: memory catalog is cheap no-LLM relevance signal. 21 new tests. Files: query-expansion.ts, memory-index.ts, search.ts, adapter.ts, recall/index.ts, tests

## Task: Diagnosis — Scribe empty responses
Root cause: agent ends turn with file edits (memory/session) but no text output. `-p` mode only outputs text to stdout. Contributing: instruction ordering puts memory updates before response, output protocol buried in long prompt.

## Task: Fix empty responses — two-phase + defensive retry
Two-phase: moved Output Protocol before Session/Memory instructions. Changed "before returning" → "after writing text response." Defensive retry: if rawOutput empty + success, fire lightweight retry asking for summary. Merge keeps original changedFiles + retry text. Files: adapter.ts, cli.ts

## Task: Make defensive retry silent
Added `silentAgents` set. Before retry, add agent to set; handleEvent returns immediately for silent agents. Files: cli.ts

## Task: Wire persona templates into CLI onboarding
New `personas.ts`: `loadPersonas()`, `scaffoldFromPersona()`. Restructured startup menu: Pick → Auto-generate → Import → Solo → Exit. Pre-TUI picker with tier grouping. `/init pick` for in-TUI selection using `askInline()`. Files: personas.ts, cli.ts, index.ts, cli/package.json

## Task: Fix defensive retry producing empty output (raw mode)
Root cause: retry used full buildTeammatePrompt (with memory instructions), causing same failure. Added `raw?: boolean` to TaskAssignment — skips prompt wrapping entirely. All 3 adapters support raw mode. Files: types.ts, adapter.ts, orchestrator.ts, cli-proxy.ts, copilot.ts, echo.ts, cli.ts

## Task: Diagnosis — "Ironic" empty response recurrence
Root cause: CLI process started before fixes were compiled. Node.js caches modules at startup — rebuilds don't affect running process. All fixes correct, just needed restart.

---
type: daily
---

# 2026-04-02

## Task: Analyze OpenClaw vs Teammates memory comparison doc
Read `docs/OpenClaw_vs_Teammates_Memory_Comparison.docx` (prepared by Scribe) and identified which OpenClaw features are worth porting to Teammates.

### Key decisions
- Research-only task; no code changes.
- Identified 4 high-value features from OpenClaw worth adding: MMR re-ranking, temporal decay, embedding cache, file watching.
- Identified 2 medium-value features: memory flush before compaction, session transcript indexing.
- Ruled out features that conflict with zero-cloud principle (multiple embedding providers, batch embedding APIs).

### Files changed
- `.teammates/beacon/memory/2026-04-02.md` — created daily log

## Task: Spec out Recall v2 features
Wrote detailed implementation specs for 5 features identified from the OpenClaw comparison. User confirmed embedding cache is skipped (we generate embeddings locally, full rebuilds basically never happen).

### Key decisions
- **Temporal Decay:** Post-retrieval score multiplier using `2^(-age/halfLife)`. Per-content-type half-lives (daily=14d, weekly=30d, monthly=90d, typed_memory=60d). Exempt Pass 1 recency results. New `temporal.ts` module in recall.
- **MMR Re-Ranking:** Greedy selection with diversity penalty. λ=0.7 default. Re-embeds result texts for inter-document similarity (~1s for 20 results). New `mmr.ts` module in recall.
- **File Watching (library API):** New `IndexWatcher` class (EventEmitter) wrapping the existing `node:fs.watch` pattern from `cmdWatch`. Enables CLI orchestrator to keep index warm in background. New `watcher.ts` module in recall.
- **Memory Flush Before Compaction:** LLM-prompted knowledge extraction before `preDispatchCompress()`. Runs as system task, parses fenced `memory` blocks from response, writes typed memories. 30-min cooldown, 30s timeout, gated behind entry threshold. New `memory-flush.ts` module in cli.
- **Session Transcript Indexing:** Opt-in (`indexTranscripts: false` default). Writes capped transcript files to `.teammates/<teammate>/transcripts/`. Auto-purge after 30 days. New `transcript-writer.ts` module in cli, `classifyUri` extended in recall.
- Build order: temporal decay → MMR → file watching → memory flush → session transcripts.

### Files changed
- `.teammates/beacon/memory/spec_recall_v2_features.md` — **new** — full implementation specs for all 5 features

## Task: Investigate system prompt splitting for coding agents
User asked whether the debug prompt file (already written to `.teammates/.tmp/debug/`) can be passed as the system prompt to coding agents instead of piping everything as one blob via stdin.

### Key decisions
- Research-only task; no code changes.
- **Claude supports `--system-prompt-file`** — confirmed via `--bare` help docs. This allows splitting the prompt into system context (identity, wisdom, team, daily logs, instructions) and user message (conversation history + task).
- **Codex, Copilot, Aider have no system prompt flag** — they must keep the current single-blob approach.
- Recommended split: system prompt = everything except `<HANDOFF_CONTEXT>` and `<TASK>`; user message = handoff context + task.
- Benefits: Anthropic prompt caching, better model attention on the task, may fix the empty-response issue from bloated prompts.
- Implementation plan: modify `buildTeammatePrompt()` to return split parts, add `supportsSystemPromptFile` to `AgentPreset`, update Claude preset to use `--system-prompt-file`.

### Files changed
- `.teammates/beacon/memory/2026-04-02.md` — updated daily log

## Task: Implement system prompt splitting for Claude
Split `buildTeammatePrompt()` to return a `PromptParts` object with `systemPrompt`, `userMessage`, and `fullPrompt`. Claude now receives identity/wisdom/daily logs/instructions via `--append-system-prompt-file` and only the task+handoff context via stdin. Other agents (Codex, Copilot, Aider) continue using the single combined prompt via stdin.

### Key decisions
- **`buildTeammatePrompt()` returns `PromptParts`** instead of `string` — has `fullPrompt` (combined, backward compat), `systemPrompt` (identity through instructions), and `userMessage` (handoff context + task).
- **Used `--append-system-prompt-file`** not `--system-prompt-file` — append preserves Claude Code's built-in system prompt (tool usage instructions, etc.) while adding teammate context on top.
- **Split boundary:** system prompt = `<IDENTITY>`, `<GOALS>`, `<WISDOM>`, `<TEAM>`, `<SERVICES>`, `<RECALL_TOOL>`, `<ENVIRONMENT>`, `<DAILY_LOGS>`, `<USER_PROFILE>`, `<RECALL_RESULTS>`, `<INSTRUCTIONS>`. User message = `<HANDOFF_CONTEXT>` + `<TASK>`.
- **System prompt file** written to `.teammates/.tmp/debug/<name>-<ts>-system.md` alongside the existing full prompt dump.
- **`AgentPreset` extended** with `supportsSystemPromptFile` boolean flag.
- **Benefits:** Anthropic prompt caching on the system prompt (identity/wisdom/logs are stable across turns), better model attention on the actual task, smaller stdin payload.
- All 312 CLI tests pass including 2 new tests for the `PromptParts` split.

### Files changed
- `packages/cli/src/adapter.ts` — `buildTeammatePrompt()` returns `PromptParts`, added `PromptParts` interface
- `packages/cli/src/adapters/cli-proxy.ts` — captures `promptParts`, writes `-system.md` file, passes `systemPromptFile` to `buildArgs` and `userMessage` to stdin
- `packages/cli/src/adapters/presets.ts` — Claude preset uses `--append-system-prompt-file`, added `supportsSystemPromptFile: true`
- `packages/cli/src/adapters/echo.ts` — updated to use `.fullPrompt` from `buildTeammatePrompt()`
- `packages/cli/src/index.ts` — exported `PromptParts` type
- `packages/cli/src/adapter.test.ts` — updated all tests for `PromptParts` return type, added 2 new tests for system/user split

## Task: Reorganize prompt pipeline — static SYSTEM-PROMPT.md + chunked recall + new budget system
Major restructuring of how prompts are built and delivered to coding agents. Three changes:

### 1. Static SYSTEM-PROMPT.md per teammate
Created `system-prompt.ts` module with `generateSystemPrompt()` that builds a static SYSTEM-PROMPT.md file for each teammate. Contains all stable prompt sections (IDENTITY, GOALS, WISDOM, TEAM, SERVICES, RECALL_TOOL, ENVIRONMENT, USER_PROFILE, INSTRUCTIONS). Generated at startup via `writeAllSystemPrompts()`, hooked into `StartupManager.startupMaintenance()`. Claude uses this file directly via `--append-system-prompt-file` instead of writing a temp file each task.

### 2. Chunked recall indexing
Created `chunker.ts` in recall with `chunkMarkdown()` that splits memory files into ~2k token chunks (~8k chars). Splits on markdown headings first, then paragraph boundaries. Each chunk is indexed as a separate Vectra document with URI suffix `#0`, `#1`, etc. Updated `Indexer.indexTeammate()`, `syncTeammate()`, and `upsertFile()` to use chunking. Search results now include `partial` and `period` metadata fields. Added `extractPeriod()`, `isChunkUri()`, `uriToRelativePath()` helpers.

### 3. New 20k token budget system
Replaced the old dual-budget system (12k daily logs + 8k recall) with a unified 20k token budget for the user message. Priority order:
1. User's message (unbounded, always included)
2. Conversation history (highest budget priority)
3. Daily log snapshot from before conversation started (medium)
4. Recalled memories in MEMORY: format (lowest)

As conversation grows, it naturally pushes out recall and daily logs. New `buildUserMessage()` function handles the budget allocation. `buildTeammatePrompt()` refactored to use pre-built system prompt content + `buildUserMessage()`. Recalled memories formatted as:
```
MEMORY:
file: <relative path>
type: <content type>
period: <time period>
partial: <true/false>
<content>
```

### Key decisions
- System prompt is a real file at `.teammates/<name>/SYSTEM-PROMPT.md`, not a temp file
- `buildTeammatePrompt()` accepts `systemPromptContent` and `systemPromptFile` options — uses pre-built content when available, falls back to inline generation
- Conversation history is now explicitly separated from the task prompt in `cli-proxy.ts` (split on `---` separator)
- `generateSystemPromptInline()` serves as fallback for tests and first-run scenarios
- The old `DAILY_LOG_BUDGET_TOKENS` constant kept for `autoCompactForBudget` compatibility
- `classifyUri()` updated to handle chunk URIs by stripping `#N` suffix
- All 1,012 tests pass (601 consolonia + 94 recall + 317 CLI)

### Files changed
- `packages/cli/src/system-prompt.ts` — **new** — static system prompt generator
- `packages/cli/src/adapter.ts` — refactored `buildTeammatePrompt()`, added `buildUserMessage()`, `formatRecallResult()`, `USER_MESSAGE_BUDGET_TOKENS`
- `packages/cli/src/adapter.test.ts` — rewritten tests for new budget system, added `buildUserMessage` and `formatRecallResult` test suites
- `packages/cli/src/adapters/cli-proxy.ts` — uses pre-built SYSTEM-PROMPT.md, extracts conversation history from prompt, passes to new `buildTeammatePrompt()` API
- `packages/cli/src/cli.ts` — imports and wires `writeAllSystemPrompts` into startup manager deps
- `packages/cli/src/startup-manager.ts` — added `generateSystemPrompts` to deps interface, calls it at start of `startupMaintenance()`
- `packages/cli/src/index.ts` — exported new functions and types
- `packages/recall/src/chunker.ts` — **new** — markdown chunking module
- `packages/recall/src/indexer.ts` — updated to chunk files before indexing
- `packages/recall/src/search.ts` — added `partial`, `period` to `SearchResult`, added `extractPeriod()`, `isChunkUri()`, `uriToRelativePath()` helpers, updated `classifyUri()` for chunk URIs
- `packages/recall/src/index.ts` — exported chunker and new search helpers

## Task: Add indexVersion to settings + gitignore SYSTEM-PROMPT.md + migration doc
Added `indexVersion` field to settings.json and startup logic so that changing the index format triggers a full rebuild of all teammate indexes on startup. Also gitignored SYSTEM-PROMPT.md and added a 0.8.0 migration step.

### Key decisions
- **`indexVersion` field** added to `.teammates/settings.json`, initial value `2` (v1 was the pre-chunking format). Stored as `INDEX_VERSION` static constant on `StartupManager`.
- **Startup check** — `checkIndexVersionChanged()` compares persisted indexVersion against `INDEX_VERSION`. If lower (or missing), `startupMaintenance()` runs `indexer.indexAll()` for a full rebuild instead of incremental sync, then persists the new version via `commitIndexVersion()`.
- **`commitVersionUpdate()` also persists indexVersion** so a version bump always brings the index version current too.
- **SYSTEM-PROMPT.md gitignored** — added `**/SYSTEM-PROMPT.md` to `.teammates/.gitignore` (generated file, not committed).
- **0.8.0 migration section** — instructs agents to add the gitignore line on upgrade.
- All 317 CLI tests pass.

### Files changed
- `.teammates/settings.json` — added `indexVersion: 2`
- `.teammates/.gitignore` — added `**/SYSTEM-PROMPT.md`
- `packages/cli/src/startup-manager.ts` — added `INDEX_VERSION`, `checkIndexVersionChanged()`, `commitIndexVersion()`, updated `commitVersionUpdate()` and `startupMaintenance()`
- `packages/cli/MIGRATIONS.md` — added `## 0.8.0` migration section for gitignore update

## Task: Rewrite WISDOM.md with distilled entries
Rewrote `.teammates/beacon/WISDOM.md` with updated compaction date (2026-04-02). Incorporated new learnings from 04-01 and 04-02 work:

### New entries added
- **Static system prompt pipeline** — two-tier architecture with SYSTEM-PROMPT.md + dynamic user message
- **20k token user message budget** — unified budget replacing old dual 12k+8k system
- **Chunked recall indexing** — `chunker.ts` splits files into ~2k token chunks
- **MEMORY: format** — structured recall result format in user message
- **SYSTEM-PROMPT.md is generated, not committed** — gitignored, regenerated at startup
- **Index versioning** — `indexVersion` in settings triggers full rebuild on format changes
- **User twin logging and compaction** — `logUserTask()` + inclusion in maintenance cycles

### Entries preserved (still valid)
- All terminal/mouse, thread, activity tracking, architecture, build/ship, and process entries retained unchanged

### Files changed
- `.teammates/beacon/WISDOM.md` — full rewrite with 2026-04-02 compaction date
- `.teammates/beacon/memory/2026-04-02.md` — updated daily log

## Task: Fix Claude activity watcher — broken regex + subagent visibility
Fixed the Claude debug log parser (`parseClaudeActivity`) which was completely non-functional. The `TOOL_USE_RE` regex was looking for a pattern (`"Getting matching hook commands for PostToolUse with query:"`) that no longer exists in Claude Code's debug output. Also added subagent activity detection and hook-free fallback signals.

### Key decisions
- **Root cause:** `TOOL_USE_RE` matched zero lines in current debug logs. The actual pattern is `"Hook PostToolUse:<TOOL> (PostToolUse)"`.
- **Fixed regex:** Added `HOOK_POSTTOOLUSE_RE` for the current format, kept `LEGACY_TOOL_USE_RE` for older versions.
- **Subagent API turn detection:** Added `SUBAGENT_API_RE` to parse `source=agent:builtin:<NAME>` API REQUEST lines. Each subagent (Explore, Plan, etc.) gets a summarized Agent event showing turn count: `Agent (Explore, 12 turns)`.
- **Hook-free Bash detection:** Added `SPAWNING_SHELL_RE` — "Spawning shell" appears for every Bash invocation regardless of hooks.
- **Hook-free Edit/Write detection:** File rename events now emit Edit events directly when no PostToolUse hook consumes them, instead of just setting `pendingFilename` that's never used.
- **Dedup logic:** "Spawning shell" Bash events are deferred and only emitted if no hook-based Bash event covers them within ±3s. File renames are consumed by hook-based Edit/Write if present, otherwise flushed as standalone Edit events.
- **Limitation:** Read, Grep, Glob have NO debug log signal when hooks are removed. These are invisible without hooks. Subagent API turn counts provide partial visibility.
- Tried to remove the dead hooks block from `.claude/settings.local.json` but got permission denied (same as Steve's twin).
- All 330 CLI tests pass (13 new tests for `parseClaudeActivity`). Clean build.

### Files changed
- `packages/cli/src/activity-watcher.ts` — rewrote `parseClaudeActivity()` with multi-signal parsing
- `packages/cli/src/activity-watcher.test.ts` — added 13 tests for `parseClaudeActivity` covering hook, subagent, hook-free, and combined scenarios
- `.teammates/beacon/memory/2026-04-02.md` — updated daily log

## Task: Replace /init with /add, /remove, /update commands
Replaced the monolithic `/init` command with three focused commands for teammate management.

### Key decisions
- **`/add`** — loads bundled personas via `loadPersonas()`, filters out already-installed teammates by checking `orchestrator.listTeammates()`, shows numbered list grouped by tier, user selects by comma-separated numbers with optional rename, scaffolds via `scaffoldFromPersona()`.
- **`/remove`** — lists only agentic teammates (filters out `type === "human"`, `adapterName`, and `selfName`), shows numbered list, requires `y` confirmation before deleting. Removes folder from disk via `rm -rf`, unregisters from Registry, and removes from status map.
- **`/update`** — matches installed teammate names against bundled persona aliases, shows numbered list of matches, supports comma-separated selection or `*` for all. Overwrites SOUL.md and WISDOM.md from persona template via new `updateFromPersona()`, preserving memory/ and other files.
- Added `Registry.unregister()` method for in-memory removal.
- Added `updateFromPersona()` to personas.ts for updating without re-scaffolding the folder structure.
- Added `removeTeammateFromDisk()` dep wired in cli.ts using `fs/promises.rm()`.
- Updated all `/init` references in onboard-flow.ts and commands.ts help text.
- All 330 CLI tests pass. Clean build.

### Files changed
- `packages/cli/src/commands.ts` — replaced `/init` command with `/add`, `/remove`, `/update`
- `packages/cli/src/personas.ts` — added `updateFromPersona()`
- `packages/cli/src/registry.ts` — added `unregister()`
- `packages/cli/src/cli.ts` — wired `removeTeammateFromDisk` dep
- `packages/cli/src/onboard-flow.ts` — updated `/init` references to `/add`
- `packages/cli/src/index.ts` — exported `updateFromPersona`

## Task: Restore PostToolUse hooks for activity tracking
Implemented a hook installer that restores visibility into Read, Grep, Glob, and other tool activity for Claude-based teammates. The hook is a no-op script — its presence triggers Claude Code to log `Hook PostToolUse:<Tool>` lines to the debug file, which `parseClaudeActivity()` already parses.

### Key decisions
- **No-op hook approach** — the hook command (`node -e "" /* teammates-activity */`) does nothing. Claude Code logs tool events to `--debug-file` whenever ANY hook is configured, and the activity watcher already parses those lines. No separate JSONL files needed.
- **Per-teammate separation via existing debug files** — each teammate already gets its own `--debug-file` path in `.teammates/.tmp/debug/`. No additional per-teammate routing needed.
- **Installed on first use** — `ensurePostToolUseHook()` is called in `CliProxyAdapter.startSession()` when `supportsDebugFile` is true (Claude preset only). Idempotent — checks for a marker string (`teammates-activity`) before adding.
- **Preserves existing settings** — reads `.claude/settings.local.json`, merges the hook into the existing `PostToolUse` catch-all matcher, writes back without touching permissions or other hooks.
- **In-process guard** — `_installed` boolean prevents redundant file I/O after first check.
- All 330 CLI tests pass. Clean build.

### Files changed
- `packages/cli/src/hook-installer.ts` — **new** — `ensurePostToolUseHook()` and `_resetHookInstallerState()`
- `packages/cli/src/adapters/cli-proxy.ts` — import + call `ensurePostToolUseHook()` in `startSession()`
- `packages/cli/src/index.ts` — exported `ensurePostToolUseHook`
- `.claude/settings.local.json` — hook entry added (via installer test run)

## Task: Add sequential handoff chain support to INSTRUCTIONS
Updated the Handoffs section in `buildInstructionLines()` to teach agents when to use parallel vs sequential (chained) handoffs. Agents now get explicit guidance: independent tasks → multiple handoff blocks (parallel); dependent tasks → single handoff with forwarding instructions embedded for the next teammate in the chain.

### Key decisions
- Added "Parallel vs Sequential Handoffs" subsection to the existing Handoffs instructions
- Sequential chains work by embedding "when done, hand off to @next-teammate" inside the first handoff's task description — no orchestrator changes needed
- Included a concrete example of the chain pattern so agents can copy it
- Decision criteria: independent = parallel, dependent = sequential, mixed = both

### Files changed
- `packages/cli/src/system-prompt.ts` — expanded handoff instructions with parallel/sequential guidance

## Task: Add isSolo setting + wordwheel for /add, /remove, /update
Two features: (1) persist the user's choice to skip adding teammates so we don't re-prompt on every startup, and (2) make `/add`, `/remove`, `/update` show a `[teammate]` placeholder and offer wordwheel completion.

### Key decisions
- **`isSolo` setting** — stored in `.teammates/settings.json` as a boolean. Set to `true` when user picks "Solo mode" (option 4) during team onboarding. Cleared to `false` when `/add` successfully adds teammates. Startup flow checks `isSolo` alongside `hasAgenticTeammates` — if both are true, skips the team onboarding prompt.
- **`readSoloSetting()` / `writeSoloSetting()`** — added to `OnboardFlow` class. `clearSoloSetting()` helper added to `commands.ts` for use after `/add`.
- **Usage strings** — changed from `/add` → `/add [teammate]`, `/remove [teammate]`, `/update [teammate]` to show the placeholder hint.
- **Commands accept args** — all three commands now accept an optional name argument. If provided, they skip the interactive numbered list and directly add/remove/update the named teammate.
- **Wordwheel completion** — extended `WordwheelView` interface with `getAvailablePersonas()` and `getRemovableTeammates()`. Added custom completion blocks in `getParamItems()` for `/add` (persona aliases not yet installed), `/remove` (removable agentic teammates), `/update` (installed teammates + `*` for all).
- **Persona caching** — `cachedPersonas` loaded once during startup in `cli.ts` and passed through the wordwheel view closures.
- All 334 tests pass, clean build.

### Files changed
- `packages/cli/src/commands.ts` — usage strings, args support, `clearSoloSetting()`, direct-name paths for all three commands
- `packages/cli/src/onboard-flow.ts` — `readSoloSetting()`, `writeSoloSetting()`, solo mode persistence
- `packages/cli/src/wordwheel.ts` — `getAvailablePersonas()`, `getRemovableTeammates()` on `WordwheelView`, completion blocks for `/add`, `/remove`, `/update`
- `packages/cli/src/cli.ts` — `cachedPersonas` property, `loadPersonas()` at startup, wired new wordwheel view methods, `isSolo` check in startup flow

### Key decisions
- Added "Parallel vs Sequential Handoffs" subsection to the existing Handoffs instructions
- Sequential chains work by embedding "when done, hand off to @next-teammate" inside the first handoff's task description — no orchestrator changes needed
- Included a concrete example of the chain pattern so agents can copy it
- Decision criteria: independent = parallel, dependent = sequential, mixed = both

### Files changed
- `packages/cli/src/system-prompt.ts` — expanded handoff instructions with parallel/sequential guidance

## Task: Fix "require is not defined" error on first launch
`printLogo()` in `onboard-flow.ts` used a CJS `require()` call to import `buildTitle` from `console/startup.js`. Since the package is `"type": "module"`, Node.js runs `.js` files as ESM where `require` is not defined. This caused a `ReferenceError` when launching `teammates claude` in any repo.

### Key decisions
- Replaced `require("./console/startup.js")` with a static ESM `import` at the top of the file
- No dynamic import needed — `buildTitle` is a pure sync function with no side effects

### Files changed
- `packages/cli/src/onboard-flow.ts` — converted `require()` to static ESM import

## Task: Bump version to 0.8.1 + ESM compliance tests
Bumped all three package versions from 0.8.0 → 0.8.1 and added unit tests to prevent CJS-in-ESM regressions.

### Key decisions
- Bumped `@teammates/cli`, `@teammates/recall`, `@teammates/consolonia` to 0.8.1
- Updated `cliVersion` in `.teammates/settings.json`
- Created `esm-compliance.test.ts` that scans ALL source files across all 3 packages
- Tests check for: bare `require()` calls, `__dirname`/`__filename` without `import.meta.url` polyfill, missing `type: module`
- Files using the standard ESM polyfill (`const __dirname = dirname(fileURLToPath(import.meta.url))`) are allowed
- All 334 CLI tests pass, clean build

### Files changed
- `packages/cli/package.json` — version 0.8.1
- `packages/recall/package.json` — version 0.8.1
- `packages/consolonia/package.json` — version 0.8.1
- `.teammates/settings.json` — cliVersion 0.8.1
- `packages/cli/src/esm-compliance.test.ts` — **new** — ESM compliance tests

## Task: Fix team onboarding not prompting on existing .teammates/ directories
The startup flow only showed the team initialization prompt when `.teammates/` didn't exist (`isNewProject`). If the directory existed but had no agentic teammates (e.g., user chose "Solo" or "Exit" on first run, or the repo was cloned with an empty `.teammates/`), the prompt was silently skipped with no way to recover — the old `/init` command was removed.

### Key decisions
- Added `hasAgenticTeammates()` to `OnboardFlow` — scans `.teammates/` for subdirectories with SOUL.md that aren't human-type, skipping `.`/`_` prefixed dirs, `example`, and the user's own avatar
- Changed startup condition from `if (isNewProject)` to `if (isNewProject || !hasTeammates)` so the team setup prompt appears whenever there are zero agentic teammates
- All 334 CLI tests pass, clean build

### Files changed
- `packages/cli/src/onboard-flow.ts` — added `hasAgenticTeammates()` method
- `packages/cli/src/cli.ts` — updated team onboarding condition

## Task: Fix multi-activity cleanup — only first [view activity] cleaned up
When multiple tasks ran concurrently and the user expanded `[show activity]` for several of them, only the first task's activity lines were properly cleaned up on completion. The rest stayed visible with duplicated/stale entries.

### Root cause
The ThreadManager's `shiftAllContainers` callback (set in its own constructor) only shifted thread container indices and pending handoff indices. It did NOT shift the ActivityManager's `lineIndices` and `blankIdx`. When the ThreadManager inserted content into the feed (displaying results, thread actions, etc.), the feed indices shifted but activity indices for other running tasks became stale. When those tasks' `cleanupActivityLines()` ran, it tried to hide lines at wrong indices.

The CLI had a wrapper getter that added activity index shifting, but it was only used by the ActivityManager's own insertions — not by the ThreadManager's insertions.

### Fix
- Moved the activity index shifting into the ThreadManager's `shiftAllContainers` callback (applied as a post-construction override after both managers exist)
- Simplified the CLI's `shiftAllContainers` getter to just delegate to the ThreadManager's now-complete callback
- This ensures ALL feed insertions — from both ThreadManager and ActivityManager — correctly shift activity indices

### Files changed
- `packages/cli/src/cli.ts` — moved activity shift logic from getter to ThreadManager callback override
